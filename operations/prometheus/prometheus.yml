global:
  evaluation_interval: 30s
  scrape_interval: 30s
  scrape_timeout: 10s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: bgmss-api
    honor_labels: false
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - api:8080
