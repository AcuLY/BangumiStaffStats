## Task Boundary

| Field | Declaration |
|---|---|
| Status | investigated: complete; specified: initial checkpoint, two semantic corrections, and external editor-state amendment approved; exact workspace-state checkpoint commit pending; implemented: partial candidates retained; verified: paused-state seals, generator-pruning diagnosis, strict validation, and independent semantic review complete; committed: planning/semantic-correction control history only, product not committed; pushed: no; released: no; deployed: no |
| Owner | Contracts implementation subagent for apply; main agent only reviews/amends OpenSpec and performs read-only acceptance; one Wave 1A finalization subagent alone stages/archives/commits after both paired acceptances |
| Writable paths | Apply product paths: `contracts/openapi/**`, `contracts/schemas/query/**`, `contracts/goldens/query/**`. Apply mutable control: checkbox markers only in `openspec/changes/define-shared-query-wire/tasks.md` |
| Read-only protected inputs | `PRODUCT.md`; `DESIGN.md`; `openspec/config.yaml`; root baseline spec; formal master/data/backend guides and data audit; `.impeccable/**`; oracle `644b7748674e553f863d0ffd61d029f86fdc0717` and named query/fixture paths; externally created untracked `.vscode/settings.json` at SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3`; all paths outside the owned/mutable set |
| Deletion complement | Empty; no tracked deletion/move/rewrite authorized. Generated `node_modules`, `.cache/npm`, `.cache/go-build`, `.cache/go-mod`, `.cache/go-path`, `.tmp` may exist transiently only below `contracts/goldens/query/` and must be removed by the reviewed bounded cleanup mode before candidate handoff. Exact sealed untracked `.vscode/settings.json` is protected foreign state and never a cleanup target |
| Mutable refs | Accepted checkpoints are `c7f868e2861e8fea250f033c27538ecf793bacad`, `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c`, and `52af379d48d7c29a479b59a4670ae07d73b1c0fb`. One workspace-state amendment subagent may advance the branch from `52af379d48d7c29a479b59a4670ae07d73b1c0fb` with exact subject `docs(openspec): record wave 1 external editor state` and the exact eight approved OpenSpec artifact paths while product/cache/temp/editor output remains unstaged and sealed. After main-agent acceptance: own task checkboxes only, index and every Git ref immutable. Finalization after both candidate acceptances may advance the branch once with the exact combined Wave 1A phase commit; no other ref |
| Consumes | `establish-formal-rewrite-baseline`; one clean approved two-change planning checkpoint; protected authorities/oracle; pinned read-only npm/Go/Unicode downloads |
| Produces | Query OpenAPI/schemas; normalized/digest/share/error/view goldens; pinned Unicode mapping; verifier/lock; Go/TS feasibility evidence; checked own tasks; synchronized archived capability during combined finalization |
| Dependencies | `establish-formal-rewrite-baseline`; paired but not apply-dependent sibling `define-archive-manifest-contract` shares only the coordination/finalization gate |
| Deliverables | All `contracts-query-wire` spec requirements |
| Acceptance | Own-path schema/OpenAPI/vector/Unicode/RFC8785/queryDigest/share gates; deterministic TS output; exact Go `models,skip-prune` output with all 17 component declarations plus syntax/format/compile smoke; no temp/cache/generated residue; index empty during apply; exact combined-envelope audit; separate main-agent acceptance; one combined archive/commit after both changes pass |
| Non-goals | Endpoint/results/runtime/store/cache lookup/statistics/catalog/Archive implementation/UI; browser acceptance is not applicable because this change creates no rendered frontend |
| Operations deferred | Push/PR/tag/release/deploy, production configs/timers/secrets/hosts/activation/monitoring/migration |
| Stop/rollback conditions | Stop on checkpoint/branch/HEAD/index/approval/dependency/protected/path/tool/vector mismatch, any drift from exact sealed `.vscode/settings.json`, or any P0/P1. Never reset-hard, checkout rollback, stash, git clean, git add -A, broad recursive delete, write/stage sibling/editor paths, external-repo mutation, or history rewrite |

Paired sibling paths tolerated but never writable by this agent are exactly `contracts/schemas/archive/**`, `contracts/goldens/archive/**`, and checkbox markers in `openspec/changes/define-archive-manifest-contract/tasks.md`.

Before this apply checklist begins, one separate delegated checkpoint subagent—not the main agent and not either apply owner—must stage only the two approved active change directories, create the reviewed planning commit, and stop. The main agent verifies that exact commit read-only and supplies its immutable HEAD/seals in the apply handoff.

### Observed correction protocol (not a task checkbox)

The first Archive-toolchain correction was accepted at `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c`. Apply later proved that exact `-generate models` against zero paths and 17 component schemas can exit zero while default pruning emits only a 190-byte comment/package file with SHA-256 `3a96d587a33aca1902c2a4325c86273647c4e7a830250a8bb8589285a99d31d0`; this is failed evidence, not a completed codegen task.

Both owners are stopped again at that exact HEAD with an empty index. Query tasks 1.1–1.3 alone are checked. For its three owned roots, the SHA-256 of `LC_ALL=C` NUL-path-sorted `shasum -a 256` lines is: 23 persistent regular files excluding `node_modules/.cache/.tmp` → `2a2483d6b91d5b764db1e6c722137fd707f5b79f8a3310b20285a291b9a5779f`; all 13,025 regular files → `84c216d846b03990ce5f7ba50fd006045df32d513fa6dcb3e15e6ba402720cf6`. The sorted `path<TAB>readlink-target<LF>` stream for seven symlinks seals to `4aa5af9e55948f608d1ef3d7f06ca5d76775946c2638deef2db7276d264fbfae`.

Archive tasks 1.1–1.4 and 2.1–2.4 alone are checked; its 11 persistent and 6,181 all-regular-file seals are `070e38ecc0a91750ffc0e98900f50f0987c26d775a85aa137d39c540c21df427` and `5b8b8801d5b672d5ffc643483d04c3c3fa239ac45015820640843520bdd71629`, and its 13-symlink seal is `8721691feb55259ac161846b32d87fce8e2fc9a4a21ffb551e8b3459f183f7ea`; `contracts/goldens/archive` is absent.

A separate quiescent correction subagent may stage exactly the eight proposal/design/spec/tasks paths for both changes, with no product/cache/temp path, and create only a commit whose sole parent is `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c` and subject is `docs(openspec): approve wave 1 query codegen correction`. It runs no product-writing command. After main-agent acceptance and unchanged seals/check sets, this original Query owner re-proves the replacement HEAD/sibling boundary and resumes at 2.1; the Archive owner resumes at 2.5. This lifecycle evidence is intentionally not another completion checkbox.

That commit is accepted as `52af379d48d7c29a479b59a4670ae07d73b1c0fb`, but before apply resumed the editor created exact untracked regular non-symlink `.vscode/settings.json` with SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3`; `.vscode/**` contains no other entry. Product seals/check sets remain unchanged. A workspace-state amendment subagent may commit only the same eight OpenSpec paths from that parent with exact subject `docs(openspec): record wave 1 external editor state`, staging/modifying no editor/product/cache/temp byte. After main-agent acceptance, task 2.1 begins by re-proving the replacement HEAD, empty index, unchanged product/sibling seals, and this sole protected tolerated editor file; any drift stops without repair.

## 1. Paired Wave 1A preflight

- [x] 1.1 Receive from the main agent the exact `WAVE1A_PLANNING_HEAD`, approval record for both change directories, protected-input hashes, and sibling envelope; run `test "$(git branch --show-current)" = codex/formal-rewrite`, `test "$(git rev-parse HEAD)" = "$WAVE1A_PLANNING_HEAD"`, and ordinary-ancestry/dependency checks, stopping before mutation on mismatch.
- [x] 1.2 Prove the checkpoint initially contains both approved active changes, `openspec status --change define-shared-query-wire --json` is apply-ready, `openspec validate --all --strict` passes, `git diff --cached --quiet` passes, and status contains no path outside the exact combined envelope at that initial time. Snapshot sibling HEAD/path hashes/status without writing a repository evidence file.
- [x] 1.3 Record the command target policy: this agent may write only its three product roots and own checkbox markers; it must not run format/cleanup/stage/restore commands over the sibling paths, and every later `git diff`, file enumeration, and test conclusion must be path-scoped.

## 2. Contracts owner — schemas and OpenAPI

- [ ] 2.1 Re-run the branch/accepted workspace-state amendment HEAD/approval/index/combined-envelope preflight; prove the sole permitted outside path is exact sealed untracked `.vscode/settings.json` and the paused product/sibling seals remain unchanged; then create closed draft-2020-12 Submitted/Effective personal-global query, `QueryDigestProjectionV1`, ranges, tag AST, PositionKey, error, share, and named operation input/view schemas only under `contracts/schemas/query/**`; bound every JSON integer to the safe interval and positive IDs/pages to `1..9007199254740991`.
- [ ] 2.2 Implement first-occurrence PositionKey deduplication semantics; exact TrimV1 boundary removal using only `U+0009..U+000D`, `U+0020`, `U+0085`, `U+00A0`, `U+1680`, `U+2000..U+200A`, `U+2028`, `U+2029`, `U+202F`, `U+205F`, and `U+3000`; pinned Unicode 15.1 assigned-scalar/NFKC/default-fold rules; recursive lone-surrogate rejection; canonical collection/tag normalization; and the cycle-free `q1:` SHA-256 projection/preimage contract. Preserve `U+FEFF`; exclude UID, dataVersion, operation/input/view, refresh/share state, search/sort/order/page/pageSize/section, revision, input/collection digests from the digest projection.
- [ ] 2.3 Create `contracts/openapi/openapi.yaml` as OpenAPI 3.1 with empty business `paths`, versioned named `$ref` components including `SharedQueryV1`, `EffectiveQueryV1`, `QueryDigestProjectionV1`, `RankingsViewV1`, `CandidatesInputV1/ViewV1`, `PersonDetailInputV1/ViewV1`, `PartnersInputV1/ViewV1`, and `CoStarInputV1/ViewV1`; encode the approved exact component field/default matrix and closed ranking plus co-star empty/partners/analysis share topology, reuse refs, and define no competing loose JSON.
- [ ] 2.4 Run JSON/YAML parse and reference enumeration with `node`, then path-scope `git diff --check -- contracts/openapi contracts/schemas/query` and prove the index remains empty and no sibling/out-of-envelope path was targeted.

## 3. Contracts owner — goldens and locked verifier

- [ ] 3.1 Re-run preflight, then add the manifest and language-neutral positive/negative/normalization/queryDigest/share/error/view vectors under `contracts/goldens/query/**`, including unknown-field injection at every layer, first-occurrence position cases, UID/exclusion invariance, every operation default, all three co-star share states, JSON-safe integer boundaries, exact digest projection/RFC 8785 JSON, the 14-byte ASCII domain plus one `0x00` (15-byte total) prefix, separator/preimage lowercase hex and unpadded base64url, exact digest, and stable errors.
- [ ] 3.2 Fetch the official Unicode 15.1 `CaseFolding.txt`, `DerivedAge.txt`, and `NormalizationTest.txt` only into their approved `contracts/goldens/query/unicode/*-15.1.0.txt` paths; preserve license/source headers; require SHA-256 respectively `4e55acfdc32825a22e87670e9056a3bf94ad7c5400065778e9e10f8314372bcf`, `04e16379344bdb9973cdb6f6bf0a5dd66f7cd41b014cd9f79d848768ae757256`, and `871238e37e3be0696ec2bd0891119a041b052da1a84485eda05a5438724b223e`; add full NFKC conformance plus exact TrimV1, assigned-age, folding, and lone-surrogate adversarial vectors. Cover `U+0085` boundary removal, `U+FEFF` preservation, and mixed boundary runs in both UID and tag cases.
- [ ] 3.3 Add exactly `package.json`, `package-lock.json`, and `verify.mjs` as committed Node tooling; lock dev-only `ajv@8.20.0`, `ajv-formats@3.0.1`, `@redocly/cli@2.40.0`, `openapi-typescript@7.13.0`, and `canonicalize@3.0.0`; reject lifecycle scripts/unlocked installs/runtime imports.
- [ ] 3.4 Make `verify.mjs` discover every declared case exactly once, strictly compile schemas, perform semantic normalization/Unicode 15.1 age/NFKC/fold, recursive scalar, RFC8785, queryDigest projection, operation-default, and share-topology checks, enforce UTF-8/safe-integer/count/catalog/path/identity limits, and provide `--cleanup-generated` that refuses symlink/path escape and removes only the six exact generated roots below its owned directory.
- [ ] 3.5 Record oracle UID/tag limit provenance without copying bulk fixtures. In the manifest record exact TS/Go generator identities, versions, full commands, OpenAPI `paths=0`, authoritative `components.schemas=17` with the exact sorted names, generated output byte lengths/SHA-256 values, and sorted declaration inventories. Require each of `CandidatesInputV1`, `CandidatesViewV1`, `CatalogContextV1`, `CoStarInputV1`, `CoStarShareWorkspaceV1`, `CoStarViewV1`, `EffectiveQueryV1`, `ErrorEnvelopeV1`, `PartnersInputV1`, `PartnersViewV1`, `PersonDetailInputV1`, `PersonDetailViewV1`, `QueryDigestProjectionV1`, `RankingShareWorkspaceV1`, `RankingsViewV1`, `SharePayloadV1`, and `SharedQueryV1` to be represented by an actual Go type declaration. Then run `git diff --check -- contracts/goldens/query` and prove all changes remain unstaged and sibling paths were not touched.

## 4. Automated and cross-language acceptance

- [ ] 4.1 Re-run preflight; require `node >=20.19.0 <21.0.0 || >=22.12.0` and `npm >=10`; set `npm_config_engine_strict=true`, `npm_config_cache="$PWD/contracts/goldens/query/.cache/npm"`, `GOCACHE="$PWD/contracts/goldens/query/.cache/go-build"`, `GOMODCACHE="$PWD/contracts/goldens/query/.cache/go-mod"`, `GOPATH="$PWD/contracts/goldens/query/.cache/go-path"`, `TMPDIR="$PWD/contracts/goldens/query/.tmp/system"`, `GOENV=off`, `GOWORK=off`, and `GOTOOLCHAIN=local`; verify every configurable effective cache/temp path is a canonical non-symlink descendant of `contracts/goldens/query/**`. Before any ordinary Go process, run the absolute Go executable's `go env GOTELEMETRY GOTELEMETRYDIR` with those Go controls inside a bootstrap macOS `sandbox-exec` profile containing `(allow default)`, `(deny network*)`, and `(deny file-write*)`. Stop on upload-enabled/unknown mode, then canonicalize and byte-seal the returned directory. For local mode, generate a reviewed later profile denying `file-write*` under that directory and run every later Go-starting command through it; never change global mode, interpret/delete counters, or authorize upload.
- [ ] 4.2 Run `npm ci --ignore-scripts --prefix contracts/goldens/query`, `node contracts/goldens/query/verify.mjs`, and `npm exec --prefix contracts/goldens/query -- redocly lint contracts/openapi/openapi.yaml`; require zero schema/OpenAPI/vector/Unicode/RFC8785/queryDigest/share errors.
- [ ] 4.3 Run `npm exec --prefix contracts/goldens/query -- openapi-typescript contracts/openapi/openapi.yaml --output contracts/goldens/query/.tmp/query.d.ts` and pinned `go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.8.0 -generate models,skip-prune -package querywire -o contracts/goldens/query/.tmp/query.gen.go contracts/openapi/openapi.yaml`; default pruning or any other generation selection is invalid. For local telemetry mode wrap every Go-starting command in the approved telemetry write-denial sandbox. Require deterministic TS output; require Go output to exceed the rejected 190-byte baseline, contain non-header type declarations for every exact 3.5 component name, and match the recorded sorted declaration inventory/byte length/hash. Run `gofmt -d` and a temporary package compile/test under `.tmp/**`; require empty format diff, successful Go syntax/compile, unchanged telemetry-directory byte seal, and no frontend/backend write. Make `verify.mjs` reject a missing declaration, mismatched inventory/count/hash/size/command, or header-only output even when the process exits zero.
- [ ] 4.4 Run `node contracts/goldens/query/verify.mjs --cleanup-generated`; prove `node_modules`, `.cache/npm`, `.cache/go-build`, `.cache/go-mod`, `.cache/go-path`, `.tmp`, generated Go/TS, and all tracked/ignored tool residue are absent from the candidate/index. Re-prove that no configurable npm/Go/temp path resolved to a default home, inherited workspace, or system cache and that the read-only telemetry-directory byte seal is unchanged; pre-existing developer caches need not be absent and cleanup must not target them.
- [ ] 4.5 Run `openspec validate --all --strict`, `git diff --check -- contracts/openapi contracts/schemas/query contracts/goldens/query openspec/changes/define-shared-query-wire/tasks.md`, exact own physical-file/manifest enumeration, `git diff --cached --quiet`, and combined-envelope status audit; record browser acceptance as not applicable and stop safely on any mismatch.

## 5. Unstaged candidate and combined finalization handoff

- [ ] 5.1 Mark only this checklist's completed boxes, prove own product/task changes are entirely unstaged, index empty, HEAD still equals the main-agent-accepted workspace-state amendment checkpoint, all outside-envelope paths absent except exact sealed untracked `.vscode/settings.json`, and every observed sibling difference remains confined to its declared envelope with no command from this agent targeting it.
- [ ] 5.2 Hand the unstaged candidate and exact commands/results to the main agent; report implemented/verified separately from committed/pushed/released/deployed, and do not stage, commit, archive, push, or start a dependent apply.
- [ ] 5.3 After this and `define-archive-manifest-contract` receive explicit separate main-agent acceptance, hand off to one quiescent finalization subagent to recheck both candidates, stage only their exact combined contract/task paths (never `git add -A`), archive/sync both changes, stage only the exact two archive trees/root specs and active-change removals, rerun combined strict/cached/path/hash checks, and stop for final main-agent read-only acceptance of the staged archive candidate.
- [ ] 5.4 Only after that final acceptance, the same finalization subagent creates the single local commit with exact subject `feat(contracts): establish wave 1 shared contracts`, then proves the planning checkpoint is its sole parent, exact approved combined path delta, both active changes absent, both archived/root specs present, a clean index and no worktree dirt except exact sealed untracked `.vscode/settings.json`, and reports committed=true while pushed/released/deployed remain false; any mismatch leaves the evidence uncommitted and stops.
