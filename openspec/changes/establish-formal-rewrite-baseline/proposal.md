## Why

The formal rewrite branch still contains the complete prototype and legacy implementation tree, while its repository-root OpenSpec control plane is only bootstrapped and untracked. A reviewed, failure-safe baseline is required before any application work so cleanup cannot discard governing documents, mix old implementation into the rewrite, or make the immutable oracle unreachable.

## What Changes

- **BREAKING**: Replace the legacy tracked tree on `codex/formal-rewrite` with an exact formal-rewrite allowlist; this intentionally removes the old frontend, backend, prototype, artifact, nested OpenSpec, and historical implementation paths from this branch without rewriting history.
- Preserve the MIT `LICENSE` blob, root `PRODUCT.md` and `DESIGN.md`, the five formal-development planning documents, the accepted data-decision audit, the root Impeccable framework, and the single repository-root OpenSpec control plane.
- Relocate `docs/decisions/prototype-data-logic-audit.md` byte-for-byte to `tmp-formal-development/decisions/` and update only its two active relative links.
- Deduplicate the identical legacy frontend/backend OpenSpec skill sets at the repository root, replace their blank configs with one reviewed root governance config, then remove every nested OpenSpec root.
- Replace the legacy `.gitignore` with a minimal rewrite-specific file that ignores generated local state without hiding Go tests, shared goldens, SQLite fixtures, example configuration, editor configuration, or future source directories.
- Correct the earlier checkpoint omission caused by the legacy `*_test.go` ignore rule: create one local supplemental evidence commit containing only `backend/internal/core/subject/rate_test.go`, fast-forward the local prototype branch to it, and keep commit `644b7748674e553f863d0ffd61d029f86fdc0717` as the unchanged outward-behavior oracle.
- Remove the rebuildable ignored trees `frontend/node_modules/` and `frontend/dist/`, then validate the physical file tree as well as the proposed Git index so ignored or untracked legacy content cannot survive cleanup.
- Preserve fail-closed execution when the safety layer rejects the reviewed `rm -rf --` form before either generated tree changes: after exact canonical/no-tracked/status checks, allow the main agent to substitute only the stricter `rm -r --` form, reseal and reapprove all governing artifacts, and allow the apply subagent to replace only the unpublished planning-approval commit while preserving its evidence parent, subject, exact 14-path delta, and four-commit final ancestry.
- Preserve the same sealed boundary when `apply_patch` rejects the tracked deletion complement during verification because named binary/non-UTF-8 blobs cannot be decoded: after proving zero complement deletion, an exact clean index, the already-approved five-path cleanup state, and the absent generated trees, allow one second and final replacement of the same unpublished planning checkpoint. UTF-8 regular files remain explicit `apply_patch` deletions; only the reviewed fixed binary/non-UTF-8 ledger may use non-recursive, non-forcing `rm --`, with one literal path per command after per-path mode/blob/SHA-256/type checks.
- Record commit `644b7748674e553f863d0ffd61d029f86fdc0717` as the immutable external visual, interaction, state, copy, responsive, and data-decision behavior oracle; old implementation is inspectable evidence only, never an architecture oracle.
- Establish mechanical preflight, allowlist, integrity, link, parser, OpenSpec, Impeccable, ancestry, operations-deferral, and safe-stop gates.
- Lock the main-agent-approved config, master plan, four change artifacts, change metadata, and six root OpenSpec skills in a reviewed SHA-256 manifest and a local planning-approval commit before cleanup.
- After main-agent acceptance of the staged cleanup candidate, create a local cleanup commit, complete and archive the change with its synchronized root capability spec and an explicit non-placeholder Purpose, create a local archive commit, and require a clean post-archive baseline before Wave 1.

Behavior classification:

- `PRESERVE_ORACLE`: no product, statistical, API, visual, interaction, copy, responsive, accessibility, or runtime behavior is changed. Future implementation compares preserved behavior against the immutable oracle commit.
- `INTENTIONAL_DELTA`: repository layout, OpenSpec location, decision-document location, and ignore rules change as required by `tmp-formal-development/formal-development-master-plan.md` and the active implementation guides.
- `NEW_CAPABILITY`: a single-root, spec-first formal-rewrite governance baseline is introduced.

## Capabilities

### New Capabilities

- `contracts-rewrite-baseline`: Defines the exact repository baseline, authority and ownership boundaries, safe cleanup behavior, retained-file integrity, oracle reachability, and failure-stop acceptance gates required before any frontend, backend, updater, or shared-contract implementation.

### Modified Capabilities

None. No previously synchronized root capability exists.

## Impact

- **Status**: specified and partially applied; supplemental evidence is committed, the first planning repair is committed, the two generated trees are removed, and the audit move/two link edits/minimal ignore file are present but unstaged. Tracked-complement deletion stopped during `apply_patch` verification with zero complement deletion because named binary/non-UTF-8 blobs are not decodable. The reviewed second and final planning repair is pending explicit main-agent reapproval. Candidate accepted, cleanup committed, archived, pushed, released, and deployed are all false.
- **Owner**: `contracts`; one implementation subagent may apply the approved tasks and a later finalization subagent may commit/archive only after the main-agent acceptance gate. The main agent may review and amend OpenSpec artifacts and performs two read-only acceptances, but does not perform cleanup, commits, archive mutation, or application implementation.
- **Writable paths**: `.gitignore`; `.codex/skills/openspec-{apply-change,archive-change,explore,propose,sync-specs,update-change}/SKILL.md`; `openspec/config.yaml`; `openspec/changes/establish-formal-rewrite-baseline/{.openspec.yaml,.approval-manifest.json,proposal.md,design.md,tasks.md,specs/contracts-rewrite-baseline/spec.md}`; post-archive `openspec/changes/archive/2026-07-23-establish-formal-rewrite-baseline/**` and `openspec/specs/contracts-rewrite-baseline/spec.md`; `tmp-formal-development/formal-development-master-plan.md`; `tmp-formal-development/decisions/prototype-data-logic-audit.md`; the two exact link-only guides `tmp-formal-development/data-logic-implementation-guide.md` and `tmp-formal-development/frontend-production-cleanup-and-architecture-plan.md`; and only the exact tracked deletion complement and generated directories declared below.
- **Read-only protected inputs**: `LICENSE`; `PRODUCT.md`; `DESIGN.md`; `.agents/skills/impeccable/**`; `.codex/hooks.json`; `.impeccable/design.json`; `tmp-formal-development/backend-development-implementation-guide.md`; and `tmp-formal-development/backend-operations-implementation-guide.md`. They are consumed and retained byte-for-byte, not writable merely because a parent directory contains writable files.
- **Deletion complement**: every oracle-tracked path not matched by the pre-archive retained allowlist, including the relocation source `docs/decisions/prototype-data-logic-audit.md` satisfied by its single move hunk, plus only the exact verified ignored generated directories `frontend/node_modules/` and `frontend/dist/`. No other untracked or ignored path is writable or deletable.
- **Mutable refs**: current `refs/heads/codex/formal-rewrite` through the four exact final local commits authorized by this change, including exactly two enumerated tool-compatibility replacements of its unpublished planning-approval checkpoint; and `refs/heads/codex/person-workbench-unified-prototype` through one compare-and-swap fast-forward from the oracle to the evidence commit. No other local or remote ref is writable.
- **Consumes**: oracle commit `644b7748674e553f863d0ffd61d029f86fdc0717`; root `PRODUCT.md` and `DESIGN.md`; the four existing `tmp-formal-development/*-guide.md` documents; `tmp-formal-development/formal-development-master-plan.md`; the accepted data-decision audit; legacy Impeccable and OpenSpec framework blobs.
- **Produces**: one repository-root OpenSpec control plane; one exact tracked and physical baseline; one local supplemental evidence commit; one planning-approval commit; one cleanup commit; one synchronized/archived OpenSpec commit; the relocated byte-identical audit; two corrected active links; a minimal `.gitignore`; a reviewed approval manifest; and machine-checkable Gate 0 evidence.
- **Dependencies**: OpenSpec CLI 1.6.0; a clean ordinary branch descended from the oracle; the formal development master plan supplied before apply; strict validation and explicit main-agent approval of proposal, spec, design, and tasks.
- **Deliverables**: the exact retained baseline and protected hashes; immutable external-behavior oracle reachability; supplemental-test evidence; single-root OpenSpec governance; archived `contracts-rewrite-baseline` root spec; clean index/worktree; and an acceptance report separating specified, applied, verified, accepted, committed, archived, pushed, released, and deployed states.
- **Acceptance**: first, the main agent read-only verifies the fully staged pre-commit cleanup candidate; second, after subagent-only cleanup and archive commits, the main agent read-only verifies exact post-archive paths, commit ancestry/deltas, approval-manifest integrity, strict OpenSpec health, and a clean worktree. Wave 1 remains blocked until both acceptances pass.
- **Non-goals**: application code, new application tests, package manifests, API schemas, shared goldens, CI, Docker, Compose, infrastructure, deployment, release, production migration, host changes, external repository changes, pushes, pull requests, tags, and oracle-history rewriting.
- **Operations deferred**: production nginx, systemd, Compose, timers, secrets, deployment/release workflows, activation, cutover, monitoring installation, real periodic runs, rollback execution, and legacy-system deletion remain outside this change.
- **Stop/rollback conditions**: stop on any root/branch/ref/status/hash/manifest/allowlist/link/parser/OpenSpec/ancestry mismatch, concurrent worktree/ref use, unapproved path or command, missing main-agent approval, or date/path drift. Preserve the workspace and report evidence; do not reset, checkout-restore, clean, rewrite the oracle/evidence/accepted history, or perform automatic destructive rollback. The only exceptions are the two exact, already-observed planning-approval replacements mechanically bounded in `tasks.md`: first the safer generated-directory command replacement, then the binary/non-UTF-8 deletion transport fallback. Neither permits unrelated drift or any further amend.

This change does not touch another repository or mutate external state. Apply is blocked until proposal, capability spec, design, and tasks are complete, strictly valid, explicitly reviewed, and approved by the main agent.
