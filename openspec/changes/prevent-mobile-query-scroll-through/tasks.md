## Task Boundary

| Field | Boundary |
| --- | --- |
| Status | Investigated and specified; implementation is blocked until this complete change passes strict validation and main-agent zero-P0/P1 review. |
| Owner | Frontend Query Workspace implementation owner. |
| Writable paths | `openspec/changes/prevent-mobile-query-scroll-through/proposal.md`; `openspec/changes/prevent-mobile-query-scroll-through/design.md`; `openspec/changes/prevent-mobile-query-scroll-through/specs/frontend-query-shell/spec.md`; `openspec/changes/prevent-mobile-query-scroll-through/tasks.md`; `frontend/src/features/query/components/QueryWorkspace.vue`; `frontend/src/shared/styles/base.css`; `frontend/tests/features/query/components.test.ts`; `frontend/tests/shared/scrollbar-system.test.ts`. |
| Read-only protected inputs | Repository authorities, accepted specs, Query Editor and compact-layout source, Person Detail Drawer source/tests/styles, oracle commit, contracts/backend/updater/operations, ignored preview files, `node_modules/`, and generated output. |
| Deletion complement | No deletion, rename, broad cleanup, or removal of existing behavior/tests. |
| Mutable refs | Compact/open Query Workspace lifecycle and exact prior inline overflow value of the actual root viewport scroll owner only; body inline overflow remains unchanged. |
| Consumes | Existing query disclosure state, compact breakpoint, local Query Editor scroll region, CSS viewport sizing, and existing frontend test harness. |
| Produces | Compact-only scroll isolation, regression coverage, and verification evidence. |
| Dependencies | Existing Vue/CSS/test stack; no package or contract change. |
| Deliverables | Strict-valid planning artifacts, RED/GREEN test, minimal component/CSS patch, completed task evidence, and unstaged final diff. |
| Acceptance | Exact commands recorded in tasks below; no cross-language contract generation is applicable because no contract changes. |
| Non-goals | Query/API/store semantics, copy, desktop changes, modal semantics, redesign, shared infrastructure, operations, or external state. |
| Operations deferred | Commit, push, PR, merge, release, deployment, live services, host, routes, and production activation are forbidden. |
| Stop/rollback conditions | Stop on branch/HEAD mismatch, unexpected dirty overlap, artifact conflict, validation failure, desktop lock, unusable mobile scroll, incorrect restoration, test failure requiring broader scope, or authority conflict. Never use `git reset --hard`, checkout rollback, `git clean`, `git add -A`, broad deletion, or external mutation. |

## 1. Frontend Query Workspace Preflight and TDD

- [x] 1.1 Verify branch `codex/post-production-frontend-fixes`, HEAD `94329c934867302113107d758e5658d7aa05cd1a`, allowed OpenSpec-only dirty state, no active conflicting change, and strict-valid reviewed artifacts before implementation; stop on mismatch.
- [x] 1.2 Add the smallest lifecycle regression in `frontend/tests/features/query/components.test.ts` and the focused mobile local-scroll CSS contract in `frontend/tests/shared/scrollbar-system.test.ts`.
- [x] 1.3 Run `cd frontend; npm exec vitest -- run tests/features/query/components.test.ts` before production edits and record RED caused by missing compact scroll isolation rather than setup or syntax. RED: 1 of 11 tests failed at the first new assertion because body overflow remained `clip` instead of `hidden`; the other 10 tests passed. The CSS contract separately failed because the compact rules still used an unbounded visible scroller.

## 2. Frontend Query Workspace Implementation

- [x] 2.1 Initial implementation: update `frontend/src/features/query/components/QueryWorkspace.vue` with a compact/expanded body-only overflow lock and exact lifecycle restoration. Accepted P1 review later established that this target is not the application's actual viewport scroll owner and requires the corrective block below.
- [x] 2.2 Update only the below-780px Query Editor rules in `frontend/src/shared/styles/base.css` so the in-flow panel remains viewport-bounded, vertically scrollable at 360px/390px and short heights, and prevents scroll chaining.
- [x] 2.3 Rerun `cd frontend; npm exec vitest -- run tests/features/query/components.test.ts tests/shared/scrollbar-system.test.ts` and record GREEN. GREEN: 2 files passed, 18 tests passed.

## 3. Frontend Acceptance and Audit

- [x] 3.1 Run relevant component/CSS/architecture checks. Focused tests passed 18/18. Under pinned Node 24.18.0/npm 11.16.0, `npm run check:architecture` ran but failed on the unchanged pre-existing rule `leaf component instantiates a store: src\app\App.vue`; this change does not touch `App.vue` or architecture boundaries.
- [x] 3.2 Run `cd frontend; npm run check`. The local Node 24.6.0 was unsuitable, so the exact pinned runtime was supplied ephemerally. The full gate stopped at the same unchanged architecture failure before later stages. Independent pinned-runtime `npm run test` passed 32 files/378 tests and `npm run typecheck` passed.
- [x] 3.3 Verify representative expanded/closed Query Workspace behavior at 360px, 390px, a short mobile height, and desktop. Browser evidence: 360x500 and 390x420 compact states had body overflow `hidden`, static in-flow overlay, local `overflow-y: auto`, `overscroll-behavior-y: contain`, and scroll content taller than its viewport; close restored body overflow and collapsed state; 1024x768 reload retained fixed desktop overlay and ordinary body overflow; console warnings/errors were empty. Raw touch injection was blocked by browser policy, so local-scroll ownership is supported by computed geometry/CSS plus regression coverage rather than synthetic touch input. Existing automated Escape/focus tests passed in the 378-test suite.
- [x] 3.4 Run `npx --yes @fission-ai/openspec@1.6.0 validate prevent-mobile-query-scroll-through --strict`, `npx --yes @fission-ai/openspec@1.6.0 validate --all --strict`, and `git diff --check`. Results: change valid; all 54 specs/changes valid; diff check passed with only expected Git LF-to-CRLF checkout warnings.
- [x] 3.5 Inspect `git status --short`, `git diff --stat`, and the full diff. Only the four declared frontend files and `openspec/changes/prevent-mobile-query-scroll-through/` changed. All changes remain unstaged, uncommitted, unpushed, unmerged, unreleased, and undeployed; the temporary preview process is stopped.

## 4. Accepted P1 Root Scroll Owner Correction

- [x] 4.1 Amend proposal, design, delta spec, and tasks to require locking/restoring the actual root viewport scroll owner (`document.scrollingElement`) while preserving body inline overflow; strict-validate the amended planning block with exact `npx --yes @fission-ai/openspec@1.6.0` before production edits. Result: `Change 'prevent-mobile-query-scroll-through' is valid`.
- [x] 4.2 Update the focused lifecycle regression first with a non-default prior inline root overflow value; assert root hidden while compact/open, exact restoration on close, breakpoint transition, and unmount, desktop unlocked, and body inline overflow preserved. RED command: `cd frontend; npm exec vitest -- run tests/features/query/components.test.ts`. Result: 1 of 11 tests failed at the first root assertion because root overflow remained `clip` instead of `hidden`; the other 10 tests passed, confirming the current body-only lock as the cause.
- [x] 4.3 Implement the minimum `QueryWorkspace.vue` correction to capture, lock, and exactly restore the same actual root scrolling element on every exit path. The body inline overflow is not mutated, and the bounded mobile local-scroller CSS remains unchanged because focused verification exposed no defect. Focused GREEN: 2 files passed, 18 tests passed.
- [x] 4.4 Run focused GREEN, full tests under Node 24.18.0/npm 11.16.0 with deterministic retry if needed, app.mount focused, typecheck, build, strict change/all validation, `git diff --check`, responsive/browser evidence where tooling permits, and final owned-path diff audit. Evidence: focused lifecycle/CSS passed 18/18; the parallel full suite passed 377/378 and hit only the existing 10s `app.mount` timeout, then `--maxWorkers=1` passed 378/378 and focused `app.mount` passed 1/1; final typecheck passed; build passed with the existing large-chunk warning. `check:architecture` and therefore `npm run check` stop on unchanged `src\app\App.vue` with `leaf component instantiates a store`. Exact strict change validation passed and all validation passed 54/54; tracked `git diff --check` passed with expected LF-to-CRLF warnings. At 390x420 the live page reported `HTML` as `document.scrollingElement`, root inline/computed overflow `hidden`, body inline overflow unchanged, root scroll height 895 over client height 420, and offset 0; Chrome wheel synthesis timed out before completing input, so no wheel/touch proof is claimed. Only the declared OpenSpec directory and four owned frontend files changed, all unstaged/uncommitted/unpushed/unmerged/unreleased/undeployed.

## 5. Mobile Single-Chrome-Layer Visual Regression Correction

- [x] 5.1 Record 390px light/dark computed and rendered evidence for the mobile Header/Query Editor paint stack, amend this same proposal/design/delta/tasks to require a transparent in-header child with unchanged Header and desktop overlay chrome, and strict-validate the change before production edits. Evidence: at 390x844, both themes rendered `.query-editor-overlay` inside `.app-header`; the Header computed to the theme's 92% chrome color plus `blur(16px) saturate(1.35)`, while the child computed to the same 92% color with `backdrop-filter: none`. Root inline/computed overflow remained `hidden`, and `.query-editor__scroll` retained `overflow-y: auto` plus `overscroll-behavior-y: contain`. Chrome policy blocked a runtime style mutation, so no override comparison is claimed. Exact `npx --yes @fission-ai/openspec@1.6.0 validate prevent-mobile-query-scroll-through --strict` passed before production edits.
- [x] 5.2 Add the smallest source-level design regression assertion first. Require the below-780px `.query-editor-overlay` override to set `background: transparent`, retain the bounded local scroll rules, and prove the parent Header plus base desktop overlay still own their accepted chrome background/backdrop. RED command: `npm exec vitest -- run tests/shared/scrollbar-system.test.ts --maxWorkers=1`. Result: 1 of 7 tests failed at the mobile overlay regex because the below-780px rule had no `background: transparent`; the other 6 tests passed, and the same assertion confirmed the parent Header and base desktop overlay still own their chrome background/backdrop.
- [x] 5.3 Implement only the below-780px `.query-editor-overlay { background: transparent; }` correction in `frontend/src/shared/styles/base.css`; do not change the Header, theme tokens, desktop overlay, or root scroll isolation. The production delta is one CSS declaration.
- [x] 5.4 Run focused GREEN; deterministic full tests under pinned Node 24.18.0/npm 11.16.0 with `--maxWorkers=1`; typecheck; production build; strict change/all validation; `git diff --check`; 390px light/dark rendered/computed verification; console audit; and final owned-path diff audit. Evidence: focused query/CSS GREEN passed 18/18. Pinned full tests passed 32 files/378 tests with `--maxWorkers=1`; standalone typecheck passed; production `npm run build` passed after using an ignored temporary wrapper so nested npm invocations stayed on Node 24.18.0/npm 11.16.0, with only the existing large-chunk warning. `npm run check` still stops immediately on unchanged `src\app\App.vue` with `leaf component instantiates a store`. Exact strict change validation passed; all validation passed 54/54; `git diff --check` passed with expected LF-to-CRLF warnings. At 390x420 in light and dark, the Header retained the theme's 92% chrome background plus `blur(16px) saturate(1.35)`, the child overlay computed transparent with no backdrop, the local scroll region computed `overflow-y: auto`/`overscroll-behavior-y: contain` with 647px scroll height over a 321px client height, the HTML scroll owner remained inline/computed `hidden` at offset 0, body inline overflow remained unchanged, and console warnings/errors were empty. Browser wheel synthesis timed out, so dynamic touch/wheel injection is not claimed; local ownership is supported by computed geometry/CSS and the regression suite. At 1024x768 the overlay remained teleported to `BODY`, fixed, and retained its own 92% chrome background/backdrop while root overflow restored to ordinary `scroll`. Screenshots are ignored under `frontend/.tmp/`. All work remains unstaged/uncommitted/unpushed/unmerged/unreleased/undeployed.
