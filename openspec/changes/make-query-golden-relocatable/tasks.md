## Task Boundary

| Boundary | Declaration |
|---|---|
| Status | Investigated and specified; not implemented, verified, committed, pushed, released, or deployed. |
| Owner | One delegated Contracts owner performs groups 1–2 without editing OpenSpec; main agent owns every task marker, group 3 audit/lifecycle, and resumption of formal acceptance. |
| Writable paths | Tracked apply: only `contracts/goldens/query/verify.mjs` plus repository-root-derived evidence and verifier self-identity in `contracts/goldens/query/manifest.json`. Main-agent lifecycle: only this change's task markers and later sync/archive paths. Transient execution: only existing exact Query roots under `contracts/goldens/query/{node_modules,.cache/npm,.cache/go-build,.cache/go-mod,.cache/go-path,.tmp}`. |
| Read-only protected inputs | All Query/OpenAPI/schema/case/Unicode/package authority not named writable; root `.gitignore`; Backend/Frontend/Updater product sources and generated files; product/design/oracle authorities; active acceptance change; other OpenSpec content during apply; refs/remotes; hosts/services; production. |
| Deletion complement | No tracked deletion. Existing bounded verifier cleanup may remove only the six approved transient leaves and exact empty `.cache` parent; clone fixtures must be inside `.tmp` and use the same no-follow cleanup boundary. |
| Mutable refs | None for groups 1–2. Only the main agent may perform separately reviewed lifecycle commits/pushes after acceptance. |
| Consumes | Accepted Query authority/toolchain, current verifier/manifest, and canonical current clone root derived from verifier location. |
| Produces | One-way `@repo-root@` evidence, closed admission/spawn guards, minimally rebound manifest, three-root/adversarial proof, and exact handoff evidence. |
| Dependencies | Accepted `contracts-query-wire` is upstream; Backend/Frontend Query consumers remain downstream and unchanged. This change must archive before `complete-integrated-development-acceptance` resumes. |
| Deliverables | Revised two tracked files, test evidence, exact diff/pointer inventory, zero residue, main-agent audit, synchronized/archived delta, and restored acceptance progress. |
| Acceptance | Strict OpenSpec; 86 old-root occurrences become zero; original and two distinct absolute clones pass with identical logical/generated bytes; negatives fail before child execution; locked Query flow passes twice; Backend/Frontend Query gates and runtime-artifact exclusion pass. |
| Non-goals | Product/wire semantics, dependencies, authority/projection, generated outputs, Go download admission, cleanup redesign, browser/UI work, operations, or external state. |
| Operations deferred | No Docker/Nginx/systemd/Compose/host/deploy/activation/retirement task. |
| Stop/rollback conditions | Stop on unreviewed artifacts, branch/HEAD/dirty mismatch, path/token ambiguity, protected edit, external identity or output drift, unsafe cleanup, residue, external mutation need, or P0/P1. Restore only exact owned unstaged preimages. |

Groups 1–2 forbid `git reset --hard`, checkout-based rollback, `git clean`,
`git add -A`, broad recursive deletion, writes outside declared paths, index/ref
mutation, and any external repository/host/service mutation.

## 1. Contracts owner — verifier and manifest

- [ ] 1.1 Preflight the exact branch and HEAD supplied by the main agent, empty
  index, allowed dirty-state inventory, absence of another owner in the two
  tracked writable files, all four reviewed change artifacts, and strict
  OpenSpec; stop without writes on any mismatch.
- [ ] 1.2 In `contracts/goldens/query/verify.mjs`, implement canonical
  verifier-location root derivation, the one-way closed `@repo-root@` evidence
  encoder, pre-child manifest shape/token/path admission, and token-free
  clone-contained spawn assertions without changing existing authority,
  codegen, Go-download, or cleanup semantics.
- [ ] 1.3 Add deterministic in-process positive and negative coverage for
  exact/packed owned paths, two synthetic roots, unknown/missing/extra/misplaced
  tokens, old or external roots, escape/non-normalized suffixes, wrong fixed
  tool/sandbox/telemetry identities, environment/argv/cwd drift, and a token at
  the spawn boundary; prove every unsafe case starts no child and changes no
  sentinel.
- [ ] 1.4 Rebind exactly the 86 original-root occurrences in
  `contracts/goldens/query/manifest.json` to admitted logical evidence and
  update only necessarily affected evidence plus verifier byte/SHA identity;
  prove zero old-root bytes and report the exact changed JSON pointers.

## 2. Contracts owner — proportional acceptance

- [ ] 2.1 Run the locked full Query install/verify, Redocly projection/bundle,
  TypeScript/Go generation, deterministic replay, Go download admission, and
  cleanup sequence twice from the original checkout; record exact commands,
  tool identities, output seals, and physical zero residue.
- [ ] 2.2 Run the same tracked candidate and committed manifest bytes from two
  distinct canonical absolute clone fixtures below the approved `.tmp` root;
  prove identical logical evidence and authority/bundle/TypeScript/Go bytes,
  then remove fixtures through the approved no-follow bounded cleanup.
- [ ] 2.3 Run unchanged Backend and Frontend Query generation checks and the
  Backend/Frontend/Updater artifact-inventory exclusion gate; browser
  acceptance is not applicable because no rendered or interactive code is
  writable.
- [ ] 2.4 Hand off unstaged and without editing OpenSpec, with strict OpenSpec,
  exact two-file product diff,
  manifest-pointer inventory, protected-input seals, empty index, physical
  residue check, and explicit state record: investigated/specified,
  implemented, and verified; not committed, pushed, released, or deployed.

## 3. Main agent — audit and lifecycle

- [ ] 3.1 Audit implementation and evidence for zero P0/P1 findings, independently
  confirm the writable/deletion complements and three-root equivalence, and
  mark only this change's completed tasks.
- [ ] 3.2 Synchronize the accepted delta into `contracts-query-wire`, archive
  this change, strictly validate all OpenSpec content, make the scoped lifecycle
  commit/push, rebuild the formal Product candidate, and only then resume
  `complete-integrated-development-acceptance`; release and deployment remain
  false.
