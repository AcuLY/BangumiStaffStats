## Task Boundary

| Boundary | Declaration |
|---|---|
| Status | investigated: complete; specified: approved; implemented: yes; verified: main diff audit plus clean install, zero-vulnerability audit, 94 tests, generation/type/build/artifact, browser, Impeccable finish, path, strict OpenSpec, and residue gates passed; committed: determined by containing Git history; pushed/released/deployed: no |
| Owner | One frontend implementation subagent owns tasks 2–5; main owns planning/acceptance/lifecycle; a separate reviewer owns read-only Impeccable finish review. |
| Writable paths | `frontend/**` and this file's task markers only during apply. |
| Stop conditions | Stop on planning HEAD/index/contract/tool/lock drift, overlapping writer, out-of-scope mutation, duplicate owner/system, direct fixture/upstream/statistics code, or failed gate. |

## 1. Planning — main

- [x] 1.1 Rebind final Node 24.18/npm 11.16, Impeccable v4 route surface, Wave 1A, current dependency versions, Hey API types-only generation, owner model, and protected paths.
- [x] 1.2 Strict-validate and approve proposal/design/spec/tasks in the shared Wave 1B planning checkpoint.

## 2. SPA and ownership — frontend owner

- [x] 2.1 Read approved OpenSpec plus Impeccable v4 new-work/operate/craft-floor; verify planning HEAD, empty index, shared query/root-spec presence, sibling boundaries, protected context, and absent `frontend/` runtime.
- [x] 2.2 Create the exact package/config/lock/docs/scripts inventory under Node `24.18.0`/npm `11.16.0` with the pinned dependency/override set and frontend-local disposable state.
- [x] 2.3 Implement one semantic SPA mount, Pinia root, Naive provider chain, bootstrap-only store, and base styles without feature/query/theme/statistical state.
- [x] 2.4 Implement architecture checks for import direction, one store/component/request system, generated-type/fetch ownership, and prototype/fixture/upstream/statistics denylists.

## 3. Transport and shared wire — frontend owner

- [x] 3.1 Implement the injected native-fetch client and bounded transport/decode errors with safe relative `/api/` validation and no real endpoint.
- [x] 3.2 Generate only `src/api/generated/query-wire/types.gen.ts` using `@hey-api/openapi-ts@0.99.0` plus bundled types plugin and `entryFile: false`; add deterministic 17-component drift check.
- [x] 3.3 Implement the sole strict Ajv query wire adapter and direct shared positive/structural-negative contract tests without business semantics or copied fixtures.

## 4. Quality and rendered handoff — frontend owner

- [x] 4.1 Run clean install and zero-vulnerability audit, generated drift, architecture/client/adapter/mount tests, TypeScript build typecheck, Vite build, artifact/bundle checks, and dependency inventory.
- [x] 4.2 Preview `/`, `/ranking`, and `/co-star` at 360px/1440px; record screenshots and verify title/main/mount/ready, no overflow/duplicate ID/console error/unhandled rejection/failed or business request.
- [x] 4.3 Verify unchanged sidecar and protected context, strict OpenSpec, Git diff/path checks, exact persistent inventory, and no node_modules/dist/coverage/cache/temp residue.
- [x] 4.4 Mark completed apply tasks and hand main an unstaged candidate plus concise command/browser evidence; do not stage, commit, archive, push, or start features.

## 5. Acceptance and lifecycle — main

- [x] 5.1 Audit the complete frontend diff and rerun material install/generation/test/type/build/artifact/browser/path/OpenSpec gates.
- [x] 5.2 Have a separate Impeccable finish reviewer inspect screenshots and context; resolve every P0/P1 and material foundation issue.
- [x] 5.3 Make only simple in-envelope corrections if needed, update status, sync/archive, stage the exact accepted envelope, and include it in the Wave 1B foundation commit.
- [x] 5.4 Verify final history and no push/release/deploy; hand the accepted foundation to later frontend feature changes.
