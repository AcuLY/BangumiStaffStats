# Verification — 2026-09-12

Implemented on master at 3f7de5c without commit, push, merge or deployment. Existing dirty brand/theme/layout/documentation changes were preserved. Additional selector and ranking changes appeared concurrently during execution and were not authored by this change.

## Implemented behavior

- Anime and game each expose main/supporting/guest/minor/narrator/voice-library/all.
- Individual scopes retain only eligible exact roles 1/2/3/4/5/6; all retains 1..6. Existing same-type cast exclusivity and all-position browse deduplication remain.
- All labels omit 仅. Details and co-star show 主役/配角/客串/闲角/旁白/声库; the frontend no longer renames 主役 to 主角 or drops minor/narrator/voice-library labels.
- Public schemas, generated consumers, fixtures, catalog generation, backend normalization and frontend catalog/recovery agree.

## Passed checks

- Pinned npm ci: Node 24.18.0 / npm 11.16.0, 242 packages. The first attempt hit a Windows module lock; the existing local frontend preview was stopped and restored with its original command on port 5174.
- Frontend focused: 50 role/detail/co-star/recovery tests plus 89 catalog/query/selector tests.
- Frontend complete test suite: 48 files / 583 tests passed with `npm run test -- --maxWorkers=2`. Initial unrestricted parallel run had two timing-sensitive integration failures; the bounded complete run passed without test or timeout changes.
- All seven frontend wire drift checks and Unicode drift check passed.
- Frontend typecheck and Vite production build passed.
- Backend current worktree `go test ./...`, `go vet ./...`, `go build ./...` passed with Go 1.26.5.
- Complete backend `scripts/check.sh` passed in an isolated copy of current backend/contracts, with pinned Node/npm/Go and shell-only LF normalization. It included generated checks, full/race/CGO-disabled tests, repeated/fuzz tests, benchmarks, vet, builds, module/license/inventory/cleanup checks. Post-gate source comparison found no content differences.
- Catalog derivation and full catalog verifier passed; query refresh/check passed (113 cases, 44,170 cross-validation executions); query-domain, person-detail, co-star and archive schemas-only validators passed. Native command adapters were temporary helpers; repository validators were not relaxed.
- OpenSpec strict validation: 98 items passed; final artifact identity synchronization does not alter specification requirements.

- Final artifact identity synchronization passed: pinned Node artifact suite 50 passed / 0 failed / 1 Windows POSIX-only skip; WSL Go 1.26.5 `go test -tags artifacts ./build` passed. Updated canonical producer runtime input manifest, OpenAPI/display/input digest pins, positive statements and linked tests. No packaging behavior changed.

## Rendered API verification

Browser plugin skill was unavailable; used existing bundled Playwright with local Chrome. A separate read-only API at 127.0.0.1:8082 and Vite at 127.0.0.1:5175 served the actual implementation against a synthetic Archive generated by the production builder. Original local and production archives were not changed.

- All six anime roles at 1440×1000 and 390×1000: catalog -> selection -> rankings -> detail returned HTTP 200, exactly one corresponding work and the actual role tag; no horizontal overflow.
- Keyboard ArrowDown/Enter selects supporting cast; voice-library selection survives reload through ordinary session recovery.
- Game narrator selection returned one narrator work and the 旁白 tag.
- All-cast ranking and co-star selection returned six common works with all six correct role labels. Default candidates displayed all cast once, without automatically duplicating individual scopes.
- Screenshots inspected: desktop minor, mobile voice-library, game narrator, and all-role co-star. Screenshots and machine-readable results are under `.tmp/cast-role-scopes/`.

## Remaining gate limitations

- Full frontend `npm run check` stops at its architecture inventory because pre-existing `src/assets/brand/bgmss-dark.svg` and `bgmss-light.svg` are not in that inventory.
- The separate production artifact check stops because the pre-existing brand replacement omits the approved brand PNG. These unrelated brand changes were not removed or rewritten.
- Current concurrent PositionCatalogBrowser changes generate a Vue onKeydown Function/Array warning. This change did not modify that event binding. Synthetic fixtures intentionally lack image resources; image-proxy 503 responses and fallback images were recorded separately from successful data endpoints.
- Temporary `contracts/schemas/catalog/tooling/node_modules/` remains untracked: verified narrow PowerShell cleanup was rejected by execution policy with only `blocked by policy`. No cleanup workaround was attempted.
- Existing running data catalogs have not been rebuilt or deployed. New choices are present in newly built catalogs; ordinary runtime activation is separate from this implementation.

Isolated QA API/preview processes were stopped after verification; the original local frontend preview remains on port 5174.

This change remains unarchived while the complete frontend gate is blocked. Accepted role-scope specs and existing scope wording are synchronized; unrelated active changes are preserved.

## Release acceptance follow-up — 2026-09-12

The user authorized all pending product/documentation commits and production synchronization. Brand ownership and artifact checks now recognize the approved light/dark SVGs; the original PNG remains reference-only. The selector keyboard binding uses one handler and no longer emits the NInput Function/Array warning.

- Full frontend `npm run check` passed on Node 24.18.0 / npm 11.16.0 with process-only `VITEST_MAX_WORKERS=2`: 583 tests, all wire and architecture checks, typecheck/build, production artifact acceptance, 8 artifact tests. Initial unrestricted execution hit resource-contention timeouts; no assertions or timeout values were weakened. Initial JavaScript gzip: 314230 bytes, below 358400.
- Repeated Linux backend complete gate, artifact Go tests, 51/51 contract artifact tests, and query-domain validator passed in an external WSL copy. Backend/contracts source comparison found no drift.
- Browser follow-up against the local API: narrator search, ArrowDown focus to the real matching option, and rendered SVG passed without console warnings/errors. Earlier desktop/mobile role QA above remains applicable.
- Operations runtime script tests passed; Docker-specific local branch was unavailable. The production preflight check passed and the currently healthy app/image remains the rollback target. Release/data snapshot saved outside the live versions directory before deployment.
- New role choices require the normal embedded updater to rebuild the Archive catalog after activation; deploying code alone is not proof that the catalog has updated.

Accepted delta requirements were compared with the three main cast-role specs and were already synchronized. Brand-only main spec/documentation wording was reconciled with the user's approved SVG change. Archive completion is part of this release preparation; CI/merge/deployment outcomes are reported separately.