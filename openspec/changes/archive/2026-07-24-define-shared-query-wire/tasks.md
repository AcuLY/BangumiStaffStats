## Task Boundary

| Field | Declaration |
|---|---|
| Status | investigated: complete; specified: contract/tooling semantics, empty-cache-parent correction, and small-correction governance approved after independent zero-finding reviews; implemented: tasks 1.1–5.2 complete, including the original six-leaf cleanup and accepted exact empty-parent correction; verified: owner schema/vector/codegen/cold-cache/cleanup/OpenSpec/envelope gates plus main and independent read-only candidate acceptance passed, with all generated/cache/temp/link/empty-directory residue absent; committed: planning/control history through `dcadeb0cb3092583b4c368767a0777cecc719596` only, Wave 1A product/finalization protocol 5.3–5.4 not committed yet; pushed: no; released: no; deployed: no |
| Owner | Contracts implementation and Wave 1A finalization remain delegated; main-agent product mutation is allowed only by the exact small-correction exception in the proposal, and stage/archive/commit remain finalizer-only |
| Writable paths | Apply product paths: `contracts/openapi/**`, `contracts/schemas/query/**`, `contracts/goldens/query/**`, and exact root `.gitignore`. Apply mutable control: checkbox markers only in `openspec/changes/define-shared-query-wire/tasks.md` |
| Read-only protected inputs | `PRODUCT.md`; `DESIGN.md`; `openspec/config.yaml`; root baseline spec; formal master/data/backend guides and data audit; `.impeccable/**`; oracle `644b7748674e553f863d0ffd61d029f86fdc0717` and named query/fixture paths; externally created untracked `.vscode/settings.json` at SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3`; concurrent spec-only `openspec/changes/bootstrap-backend-runtime/**`, `openspec/changes/bootstrap-updater-runtime/**`, and `openspec/changes/bootstrap-frontend-foundation/**`; all paths outside the owned/mutable set |
| Deletion complement | Every path outside the three contract roots, own task markers, and exact root `.gitignore` rewrite. No contract deletion/move is authorized. Generated `node_modules`, `.cache/npm`, `.cache/go-build`, `.cache/go-mod`, `.cache/go-path`, `.tmp` may exist transiently only below `contracts/goldens/query/` and must be removed by the reviewed bounded cleanup mode before candidate handoff. Exact sealed untracked `.vscode/settings.json` and every other root/framework/sibling path remain protected and never a cleanup/ignore target |
| Mutable refs | Accepted checkpoints are `c7f868e2861e8fea250f033c27538ecf793bacad`, `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c`, `52af379d48d7c29a479b59a4670ae07d73b1c0fb`, and editor-state amendment `dcadeb0cb3092583b4c368767a0777cecc719596`. Product writes are limited to the exact apply product paths; only own checkbox markers may change within OpenSpec control state. Index and every Git ref remain immutable. Finalization after both candidate acceptances may advance the branch once with the exact combined Wave 1A phase commit; no other ref |
| Consumes | `establish-formal-rewrite-baseline`; one clean approved two-change planning checkpoint; protected authorities/oracle; pinned read-only npm/Go/Unicode downloads |
| Produces | Query OpenAPI/schemas; normalized/digest/share/error/view goldens; pinned Unicode mapping; verifier/lock; Go/TS feasibility evidence; checked own tasks; synchronized archived capability during combined finalization |
| Dependencies | `establish-formal-rewrite-baseline`; paired but not apply-dependent sibling `define-archive-manifest-contract` shares only the coordination/finalization gate |
| Deliverables | All `contracts-query-wire` spec requirements |
| Acceptance | Own-path schema/OpenAPI/vector/Unicode/RFC8785/queryDigest/share gates; deterministic TS output from authority; exact A/B sanitized codegen-only source projections and independent DAG expansion; Redocly telemetry/config/network isolation; byte-identical fully dereferenced JSON bundles with zero ref/resource/anchor keywords and canonical public-schema equivalence; exact Go `models,skip-prune` output from bundle A with all 17 public declarations plus syntax/format/compile smoke; exact root ignore positive/negative matrix plus no physical temp/cache/generated residue; index empty during apply; exact combined-envelope audit; separate main-agent acceptance; one combined archive/commit after both changes pass |
| Non-goals | Endpoint/results/runtime/store/cache lookup/statistics/catalog/Archive implementation/UI; browser acceptance is not applicable because this change creates no rendered frontend |
| Operations deferred | Push/PR/tag/release/deploy, production configs/timers/secrets/hosts/activation/monitoring/migration |
| Stop/rollback conditions | Stop on checkpoint/branch/HEAD/index/approval/dependency/protected/path/tool/vector mismatch outside the exact apply/sibling/spec-only planning/editor allowances, any drift from exact sealed `.vscode/settings.json`, or any P0/P1. Never reset-hard, checkout rollback, stash, git clean, git add -A, broad recursive delete, write/stage sibling/planning/editor paths, external-repo mutation, or history rewrite |

Paired sibling paths tolerated but never writable by this agent are exactly `contracts/schemas/archive/**`, `contracts/goldens/archive/**`, and checkbox markers in `openspec/changes/define-archive-manifest-contract/tasks.md`.

Concurrent Wave 1B planning paths tolerated but never writable/input/dependency evidence for this agent are exactly `openspec/changes/bootstrap-backend-runtime/**`, `openspec/changes/bootstrap-updater-runtime/**`, and `openspec/changes/bootstrap-frontend-foundation/**`. They may contain only OpenSpec artifacts and remain excluded from Wave 1A staging/finalization.

Before this apply checklist begins, one separate delegated checkpoint subagent—not the main agent and not either apply owner—must stage only the two approved active change directories, create the reviewed planning commit, and stop. The main agent verifies that exact commit read-only and supplies its immutable HEAD/seals in the apply handoff.

### Observed correction protocol (not a task checkbox)

The first Archive-toolchain correction was accepted at `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c`. Apply later proved that exact `-generate models` against zero paths and 17 component schemas can exit zero while default pruning emits only a 190-byte comment/package file with SHA-256 `3a96d587a33aca1902c2a4325c86273647c4e7a830250a8bb8589285a99d31d0`; this is failed evidence, not a completed codegen task.

Both owners are stopped again at that exact HEAD with an empty index. Query tasks 1.1–1.3 alone are checked. For its three owned roots, the SHA-256 of `LC_ALL=C` NUL-path-sorted `shasum -a 256` lines is: 23 persistent regular files excluding `node_modules/.cache/.tmp` → `2a2483d6b91d5b764db1e6c722137fd707f5b79f8a3310b20285a291b9a5779f`; all 13,025 regular files → `84c216d846b03990ce5f7ba50fd006045df32d513fa6dcb3e15e6ba402720cf6`. The sorted `path<TAB>readlink-target<LF>` stream for seven symlinks seals to `4aa5af9e55948f608d1ef3d7f06ca5d76775946c2638deef2db7276d264fbfae`.

Archive tasks 1.1–1.4 and 2.1–2.4 alone are checked; its 11 persistent and 6,181 all-regular-file seals are `070e38ecc0a91750ffc0e98900f50f0987c26d775a85aa137d39c540c21df427` and `5b8b8801d5b672d5ffc643483d04c3c3fa239ac45015820640843520bdd71629`, and its 13-symlink seal is `8721691feb55259ac161846b32d87fce8e2fc9a4a21ffb551e8b3459f183f7ea`; `contracts/goldens/archive` is absent.

A separate quiescent correction subagent may stage exactly the eight proposal/design/spec/tasks paths for both changes, with no product/cache/temp path, and create only a commit whose sole parent is `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c` and subject is `docs(openspec): approve wave 1 query codegen correction`. It runs no product-writing command. After main-agent acceptance and unchanged seals/check sets, this original Query owner re-proves the replacement HEAD/sibling boundary and resumes at 2.1; the Archive owner resumes at 2.5. This lifecycle evidence is intentionally not another completion checkbox.

That commit is accepted as `52af379d48d7c29a479b59a4670ae07d73b1c0fb`, but before apply resumed the editor created exact untracked regular non-symlink `.vscode/settings.json` with SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3`; `.vscode/**` contains no other entry. Product seals/check sets remained unchanged. The eight-path workspace-state amendment from that parent was accepted as `dcadeb0cb3092583b4c368767a0777cecc719596` with exact subject `docs(openspec): record wave 1 external editor state`, staging/modifying no editor/product/cache/temp byte. Task 2.1 then re-proved the replacement HEAD, empty index, unchanged product/sibling seals, and this sole protected tolerated editor file; any drift still stops without repair.

## 1. Paired Wave 1A preflight

- [x] 1.1 Receive from the main agent the exact `WAVE1A_PLANNING_HEAD`, approval record for both change directories, protected-input hashes, and sibling envelope; run `test "$(git branch --show-current)" = codex/formal-rewrite`, `test "$(git rev-parse HEAD)" = "$WAVE1A_PLANNING_HEAD"`, and ordinary-ancestry/dependency checks, stopping before mutation on mismatch.
- [x] 1.2 Prove the checkpoint initially contains both approved active changes, `openspec status --change define-shared-query-wire --json` is apply-ready, `openspec validate --all --strict` passes, `git diff --cached --quiet` passes, and status contains no path outside the exact combined envelope at that initial time. Snapshot sibling HEAD/path hashes/status without writing a repository evidence file.
- [x] 1.3 Record the command target policy: this agent may write only its three Contracts product roots, the exact root `.gitignore`, and own checkbox markers; it must not run format/cleanup/stage/restore commands over the sibling paths, and every later `git diff`, file enumeration, and test conclusion must be path-scoped.

## 2. Contracts owner — schemas and OpenAPI

- [x] 2.1 Re-run the branch/accepted workspace-state amendment HEAD/approval/index/combined-envelope preflight; prove the sole permitted outside path is exact sealed untracked `.vscode/settings.json` and the paused product/sibling seals remain unchanged; then create closed draft-2020-12 Submitted/Effective personal-global query, `QueryDigestProjectionV1`, ranges, tag AST, PositionKey, error, share, and named operation input/view schemas only under `contracts/schemas/query/**`; bound every JSON integer to the safe interval and positive IDs/pages to `1..9007199254740991`.
- [x] 2.2 Implement first-occurrence PositionKey deduplication semantics; exact TrimV1 boundary removal using only `U+0009..U+000D`, `U+0020`, `U+0085`, `U+00A0`, `U+1680`, `U+2000..U+200A`, `U+2028`, `U+2029`, `U+202F`, `U+205F`, and `U+3000`; pinned Unicode 15.1 assigned-scalar/NFKC/default-fold rules; recursive lone-surrogate rejection; canonical collection/tag normalization; and the cycle-free `q1:` SHA-256 projection/preimage contract. Preserve `U+FEFF`; exclude UID, dataVersion, operation/input/view, refresh/share state, search/sort/order/page/pageSize/section, revision, input/collection digests from the digest projection.
- [x] 2.3 Create `contracts/openapi/openapi.yaml` as OpenAPI 3.1 with empty business `paths`, versioned named `$ref` components including `SharedQueryV1`, `EffectiveQueryV1`, `QueryDigestProjectionV1`, `RankingsViewV1`, `CandidatesInputV1/ViewV1`, `PersonDetailInputV1/ViewV1`, `PartnersInputV1/ViewV1`, and `CoStarInputV1/ViewV1`; encode the approved exact component field/default matrix and closed ranking plus co-star empty/partners/analysis share topology, reuse refs, and define no competing loose JSON.
- [x] 2.4 Run JSON/YAML parse and reference enumeration with `node`, then path-scope `git diff --check -- contracts/openapi contracts/schemas/query` and prove the index remains empty and no sibling/out-of-envelope path was targeted.

## 3. Contracts owner — goldens and locked verifier

- [x] 3.1 Re-run preflight, then add the manifest and language-neutral positive/negative/normalization/queryDigest/share/error/view vectors under `contracts/goldens/query/**`, including unknown-field injection at every layer, first-occurrence position cases, UID/exclusion invariance, every operation default, all three co-star share states, JSON-safe integer boundaries, exact digest projection/RFC 8785 JSON, the 14-byte ASCII domain plus one `0x00` (15-byte total) prefix, separator/preimage lowercase hex and unpadded base64url, exact digest, and stable errors.
- [x] 3.2 Fetch the official Unicode 15.1 `CaseFolding.txt`, `DerivedAge.txt`, and `NormalizationTest.txt` only into their approved `contracts/goldens/query/unicode/*-15.1.0.txt` paths; preserve license/source headers; require SHA-256 respectively `4e55acfdc32825a22e87670e9056a3bf94ad7c5400065778e9e10f8314372bcf`, `04e16379344bdb9973cdb6f6bf0a5dd66f7cd41b014cd9f79d848768ae757256`, and `871238e37e3be0696ec2bd0891119a041b052da1a84485eda05a5438724b223e`; add full NFKC conformance plus exact TrimV1, assigned-age, folding, and lone-surrogate adversarial vectors. Cover `U+0085` boundary removal, `U+FEFF` preservation, and mixed boundary runs in both UID and tag cases.
- [x] 3.3 Add exactly `package.json`, `package-lock.json`, and `verify.mjs` as committed Node tooling; lock dev-only `ajv@8.20.0`, `ajv-formats@3.0.1`, `@redocly/cli@2.40.0`, `openapi-typescript@7.13.0`, and `canonicalize@3.0.0`; reject lifecycle scripts/unlocked installs/runtime imports.
- [x] 3.4 Make `verify.mjs` discover every declared case exactly once, strictly compile schemas, perform semantic normalization/Unicode 15.1 age/NFKC/fold, recursive scalar, RFC8785, queryDigest projection, operation-default, and share-topology checks, enforce UTF-8/safe-integer/count/catalog/path/identity limits, and provide fail-closed cleanup support for only the six exact generated roots below its canonical owned directory. Cleanup safety must use `path.relative` containment and segment-by-segment `lstat` from the repository/golden root through each exact target; only `ENOENT` means absent, while every real or dangling root/ancestor symlink fails. Internal symlinks are unlink-only leaves and are never followed, recursed, `stat`-resolved, or chmodded; owner `rwx` repair applies only to real directories. Delete present roots with recursive `force=false`, `maxRetries=5`, and `retryDelay=100`, require a post-delete `lstat` result of `ENOENT`, and report exact sorted `removed` and `alreadyAbsent` lists. Add a static/synthetic mode under `.tmp/cleanup-safety/**` that runs before real cleanup and proves nested read-only removal, internal-link no-follow with an unchanged outside-target sentinel, exact-root and dangling-ancestor rejection, failure/postcondition behavior, and zero synthetic residue without repointing or trialing any live allowlist root. Replace cache-dependent accepted-command `stderrBytes` fields with stable policy `go-download-progress-v1` plus the exact lexically sorted union allowlist of complete `module@version` pairs from the pinned generator tool graph and temporary compile graph. Apply the policy only to final status-zero candidate generation/replay/compile evidence; keep earlier rejected and corrected recovery attempts in a separate history namespace that neither satisfies nor fails candidate admission.
- [x] 3.5 Record oracle UID/tag limit provenance without copying bulk fixtures. Rewrite root `.gitignore` to the exact UTF-8/LF bytes below, including the final LF and no other line: `# macOS`, `.DS_Store`, blank, `# Local secrets and environment overrides`, `.env`, `.env.*`, `!.env.example`, `!.env.*.example`, blank, `# Query contract tool state; physically absent at candidate handoff`, `/contracts/goldens/query/node_modules/`, `/contracts/goldens/query/.cache/npm/`, `/contracts/goldens/query/.cache/go-build/`, `/contracts/goldens/query/.cache/go-mod/`, `/contracts/goldens/query/.cache/go-path/`, `/contracts/goldens/query/.tmp/`. In the manifest record its exact bytes/hash; exact TS/verifier-projection/Redocly/Go identities, versions, full argv/environments/sandbox profiles; authoritative source `paths=0` and `components.schemas=17` with exact sorted names; ref-DAG/key/closed-tree evidence; A/B projection tree hashes and exact 14 root-key deletions; A/B byte-identical bundle bytes/SHA-256/zero-key/helper inventories; response-name/non-schema-field/expanded-schema equivalence evidence; independent-expansion and all-golden equivalence evidence; generated output bytes/SHA-256 values; and sorted declaration inventories. Require each of `CandidatesInputV1`, `CandidatesViewV1`, `CatalogContextV1`, `CoStarInputV1`, `CoStarShareWorkspaceV1`, `CoStarViewV1`, `EffectiveQueryV1`, `ErrorEnvelopeV1`, `PartnersInputV1`, `PartnersViewV1`, `PersonDetailInputV1`, `PersonDetailViewV1`, `QueryDigestProjectionV1`, `RankingShareWorkspaceV1`, `RankingsViewV1`, `SharePayloadV1`, and `SharedQueryV1` to be represented by an actual Go type declaration. Then run `git diff --check -- .gitignore contracts/goldens/query`, prove every change remains unstaged, and prove no sibling/editor path was touched or newly hidden.

## 4. Automated and cross-language acceptance

- [x] 4.1 Re-run preflight; preserve the package's declared compatibility range `node >=20.19.0 <21.0.0 || >=22.12.0` / `npm >=10`, but run this 2026 acceptance only with supported-LTS `/Users/luca/.nvm/versions/node/v24.16.0/bin/node` and its npm `11.13.0`, never the ambient EOL Node 20 default. Require `bin/node` to be a regular non-symlink executable and expected `bin/npm` to canonicalize without escape to the regular same-root npm CLI; seal both resolved files and prepend only that exact bin to the child `PATH`. Set `npm_config_engine_strict=true`, `npm_config_cache="$PWD/contracts/goldens/query/.cache/npm"`, `GOCACHE="$PWD/contracts/goldens/query/.cache/go-build"`, `GOMODCACHE="$PWD/contracts/goldens/query/.cache/go-mod"`, `GOPATH="$PWD/contracts/goldens/query/.cache/go-path"`, `TMPDIR="$PWD/contracts/goldens/query/.tmp/system"`, `GOENV=off`, `GOWORK=off`, and `GOTOOLCHAIN=local`; verify every configurable effective cache/temp path is a canonical non-symlink descendant of `contracts/goldens/query/**`. In the combined-envelope audit, tolerate but never target the three exact spec-only Wave 1B planning directories declared above; reject any other new path or any non-OpenSpec output inside them. Preserve the already completed first/only bootstrap result `local` at canonical `/Users/luca/Library/Application Support/go/telemetry`; do not rerun `go env`. Treat the observed global telemetry snapshot as diagnostic only because persistent `gopls`/`vscgo` processes can change it; do not require global seal equality, claim exclusive attribution, or terminate/configure user processes. Prove the external collection-client Go owner remains interrupted. Generate a reviewed `/usr/bin/sandbox-exec` profile containing `(version 1)`, `(allow default)`, and `(deny file-write* (subpath "<canonical telemetry directory>"))`; require every later Query command capable of starting Go, including nested verifier commands, to run through it. Record exact wrapper/profile/child argv and stop without fallback on direct invocation, missing inheritance, path/profile mismatch, or wrapper failure; never change global mode, interpret/delete counters, authorize upload, or write the telemetry directory.
- [x] 4.2 Run `npm ci --ignore-scripts --prefix contracts/goldens/query` and exact-Node `verify.mjs`. Make the verifier prove every source `$ref` resolves inside the seven-schema/OpenAPI authority; the graph is acyclic; every `$ref` object is sibling-free; and `$dynamicRef`, `$recursiveRef`, `$anchor`, `$dynamicAnchor`, nested `$id`, and nested `$schema` are absent. Run its bounded `--prepare-codegen-projections` mode to create exactly `.tmp/codegen-{a,b}/{redocly.yaml,source/openapi/openapi.yaml,source/schemas/query/<7 exact files>}` before bundles: configs are exact `{}\n`, OpenAPI bytes equal authority, each temporary schema deletes only its one exact root `$id` and `$schema`, A/B trees byte-match, and no `.redocly.lint-ignore.yaml` exists in source/projection/config discovery scope. Seal the exact Redocly CLI file/version. Through exact Node 24.16.0 and a reviewed sandbox profile containing exactly `(version 1)`, `(allow default)`, and `(deny network*)`, run every Redocly version/lint call with clean owned HOME/TMPDIR, `REDOCLY_TELEMETRY=off`, explicit `--config .tmp/codegen-a/redocly.yaml`, and exact lint override `--extends recommended`; require zero schema/OpenAPI/vector/Unicode/RFC8785/queryDigest/share errors and source lint errors, no successful network operation, and no external/default-home write. A sandbox denial or nonzero child fails acceptance; absence of a denial message alone SHALL NOT be recorded as proof that no network attempt occurred.
- [x] 4.3 Generate two TS outputs from authoritative `contracts/openapi/openapi.yaml` with exact `openapi-typescript@7.13.0` and require byte identity. For each A/B tree, invoke the sealed `node_modules/@redocly/cli/bin/cli.js` directly through exact Node 24.16.0 under the clean owned environment and same exact network-denial sandbox with `REDOCLY_TELEMETRY=off`, its explicit projection-local empty config, and exact `bundle <projection>/source/openapi/openapi.yaml --dereferenced --ext json --component-names-strategy basename --component-renaming-conflicts-severity error --remove-unused-components=false --keep-url-references=false --output <projection>/query.bundle.json`; npm exec, config discovery, lint-ignore, telemetry, successful network, plain/unsanitized bundling, import mapping, or another bundler is invalid. Require A/B bundle bytes equal. Run `verify.mjs --verify-codegen-projections` to require recursive zero `$ref`/`$dynamicRef`/`$recursiveRef`/`$id`/`$schema`/`$anchor`/`$dynamicAnchor`, exact OpenAPI metadata/zero paths/17 public names, exact helper inventory, canonical deep equality of every public schema with the verifier's independent cycle-safe expansion, exact authority response names plus `description`/`x-error-codes`/media-type keys, each response `application/json.schema` canonical-deep-equal to the independent `ErrorEnvelopeV1` expansion, no other response fields, and authority/A/B agreement for every golden's accept/reject, stable classification, and instance path. Do not require dereferenced response objects to deep-equal source objects that contain `$ref`. Then run pinned `go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.8.0 -generate models,skip-prune -package querywire -o contracts/goldens/query/.tmp/query.gen.go contracts/goldens/query/.tmp/codegen-a/query.bundle.json`; direct authority/B input, default pruning, import mapping, or other selection is invalid. Wrap every Go-starting command in the approved telemetry write-denial sandbox. Require output above the rejected 190-byte baseline, actual declarations for every 3.5 public name, exact inventory/bytes/hash, empty wrapped `gofmt -d`, successful wrapped temporary compile/test, and no frontend/backend/telemetry write. Make the verifier reject any projection/bundle/Go/wrapper false pass even when a child exits zero.
  The checked 4.3 marker records successful mechanical generation/replay/format/compile execution and its deterministic product bytes only. Cold-cache stderr admission and its manifest representation remain pending under tasks 3.4 and 5.2; this marker does not claim that policy is already implemented.
- [x] 4.4 First run the static/synthetic cleanup-safety mode and require every read-only/internal-link/root-link/dangling-ancestor/sentinel/postcondition case to pass with no fixture residue. Only then run `node contracts/goldens/query/verify.mjs --cleanup-generated` exactly once. Require per-segment `lstat`/containment checks, internal-link unlink-only behavior, directory-only owner permission repair, bounded recursive `force=false` deletion with `maxRetries=5` and `retryDelay=100`, per-root post-delete `ENOENT`, and exact sorted `removed` versus `alreadyAbsent` output. Prove `node_modules`, `.cache/npm`, `.cache/go-build`, `.cache/go-mod`, `.cache/go-path`, `.tmp` including both projections/configs/bundles/profiles/homes, generated Go/TS, and every other ignored/tool residue are physically and index absent even though root `.gitignore` covers the six exact Query transient paths. Run `git check-ignore --no-index -v` positive checks for `.DS_Store`, `.env`, a representative `.env.*`, and those six paths; verify `.env.example` and `.env.*.example` are visible exceptions. Run negative checks proving representative persistent Query files, root/product locks, source/tests, all OpenSpec, frontend/backend/updater product paths, unexpected logs/build/cache paths, and exact `.vscode/settings.json` are not ignored, with only the exact preserved macOS/environment classes excluded from that negative universe. Re-prove no configurable npm/Go/Redocly/temp/home path escaped, every Redocly call had clean config/telemetry/network isolation, every Query Go-starting command used the approved wrapper, no Query child completed a telemetry write, no outside-target sentinel changed, and no cleanup command targeted global telemetry or another external directory; pre-existing developer caches and concurrent editor telemetry need not be absent or byte-stable.
- [x] 4.5 Run `openspec validate --all --strict`, `git diff --check -- .gitignore contracts/openapi contracts/schemas/query contracts/goldens/query openspec/changes/define-shared-query-wire/tasks.md`, exact own physical-file/manifest enumeration including the one root ignore file, positive/negative ignore matrix, `git diff --cached --quiet`, and combined-envelope status audit; record browser acceptance as not applicable and stop safely on any mismatch.
- [x] 4.6 Correct the empty-cache-parent defect without rerunning product generation. First re-prove HEAD/index/combined envelope, all six exact live leaves physically and index absent, exact empty real non-symlink `contracts/goldens/query/.cache`, and byte-identical hashes for the other 22 Query product files. Modify only `verify.mjs` plus exactly `acceptanceEvidence.projectionTool.verifier.{bytes,sha256}` in `manifest.json`; only this task checkbox may change in OpenSpec. Restrict the verifier diff to generated-root/empty-parent cleanup helpers, cleanup-safety cases/lifecycle, cleanup dispatch, and cleanup evidence output; reject any schema/vector/normalization/Unicode/share/codegen/Go-stderr-admission or other unrelated diff.

  The prune SHALL use `path.relative` and segment `lstat`, classify only initial exact `ENOENT` as `alreadyAbsent`, require a real non-symlink zero-entry directory, and remove it non-recursively. Permit one initial removal plus at most five retries, each after exactly 100 ms and only for removal error `EBUSY`, `EMFILE`, `ENFILE`, or `EPERM`; before each retry repeat exact `lstat` and zero-entry `readdir`. `ENOTEMPTY`, an `ENOENT` race, entry/link/type/identity drift, escape, other error, exhausted retries, or a non-`ENOENT` postcondition SHALL fail immediately without recursive deletion.

  Extend `--verify-cleanup-safety` with empty-parent success and non-empty-parent rejection/preservation. Safety preflight SHALL require exact `.tmp` `ENOENT`; the mode may create only exact `.tmp` as its synthetic container, must remove all fixtures/children, then non-recursively remove that exact `.tmp` through the bounded primitive and prove `lstat -> ENOENT`. After independent OpenSpec approval, run only that safety mode and one narrowly authorized corrected cleanup invocation; require all six leaves `alreadyAbsent`, `.cache` under separate `emptyParents.removed`, no physical/index cache/temp/generated/symlink residue, and no outside sentinel change. Do not run npm, Go, Redocly, TypeScript generation, full verifier, schema/vector code, another install, or any other product-writing command. Use read-only diff/hash checks to prove the manifest changed only the two named scalar values and the other 22 products stayed byte-identical; then run only read-only strict OpenSpec/path/ignore/index/HEAD/envelope checks and stop unstaged. Do not edit proposal/design/delta-spec status text.

## 5. Unstaged candidate and combined finalization handoff

- [x] 5.1 Mark only this checklist's completed boxes, prove own product/task changes are entirely unstaged, index empty, HEAD still equals the main-agent-accepted workspace-state amendment checkpoint, all outside-envelope paths absent except the three exact spec-only Wave 1B planning directories and exact sealed untracked `.vscode/settings.json`, and every observed sibling/planning difference remains confined to its declared read-only envelope with no command from this agent targeting it.
- [x] 5.2 Hand the unstaged candidate and exact commands/results to the main agent, including each final candidate-success generation/replay/compile child's actual stderr bytes and `go-download-progress-v1` result while keeping historical recovery attempts separately identified; report implemented/verified separately from committed/pushed/released/deployed, and do not stage, commit, archive, push, or start a dependent apply.
- 5.3 After this and `define-archive-manifest-contract` receive explicit separate main-agent acceptance, hand off to one quiescent finalization subagent to recheck both candidates, validate both active change IDs, stage only their exact combined Contracts roots, root `.gitignore`, and checked-task paths (never `git add -A`), synchronize/archive both changes, explicitly stage only the exact two archive trees/root specs and active-change removals, validate both synchronized root capability IDs plus repository-wide OpenSpec/doctor, verify recorded successful product evidence and byte-identical candidate seals without recreating cleaned disposable roots, rerun combined strict/cached/unstaged/path/hash checks, and stop for final main-agent read-only acceptance of the staged archive candidate. Exact Wave 1B planning directories and `.vscode/**` remain unstaged, unmodified foreign state.
- 5.4 Only after that final acceptance, the same finalization subagent creates the single local commit with exact subject `feat(contracts): establish wave 1 shared contracts`, then proves the planning checkpoint is its sole parent, exact approved combined path delta, both active changes absent, both archived/root specs present, a clean index and no worktree dirt except the three exact spec-only Wave 1B planning directories plus exact sealed untracked `.vscode/settings.json`, and reports committed=true while pushed/released/deployed remain false; any mismatch leaves the evidence uncommitted and stops.

Items 5.3–5.4 are an independent finalization protocol outside this change's completion checkbox set. The Query apply owner MUST NOT execute them; they are intentionally non-checkbox lifecycle steps because the active change will have moved before commit proof can exist.
