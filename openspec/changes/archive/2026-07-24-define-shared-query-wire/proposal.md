## Why

The clean-room frontend and Go backend need one versioned, language-neutral query contract before either runtime can be bootstrapped. Without it, personal/global isolation, stable position identities, tag semantics, view operations, error handling, and share replay would be redefined independently by each consumer.

## What Changes

- Add the `contracts-query-wire` capability as the machine-readable authority for the v1 shared query, query-adjacent view values, error envelope, and self-contained share fragment.
- Define a strict `scope=personal|global` discriminated union. Personal requests explicitly carry the public UID, collection statuses, and personal-only filters; global requests reject every personal-only field.
- Define opaque, stable `PositionKey` values for exact staff, cast main/all, and the dormant future staff-set namespace. Cross-type keys, non-selectable keys, and same-type cast main/all conflicts are explicit failures; repeated keys normalize by retaining the first occurrence so semantic order remains stable.
- Replace the oracle's UI-only enabled/range/tag representation with normalized wire values: omitted inactive ranges, explicit tag AST, ordered positions, canonical unordered sets, stable defaults, and no `/` or `+` syntax on the wire.
- Define shared search/order/page/page-size primitives and a closed per-operation component matrix with exact fields, scope/section rules, and defaults that both share v1 and future endpoint contracts must reuse, without defining endpoint responses or statistical results.
- Define the cycle-free Effective Query normalization and digest-projection pipeline plus versioned SHA-256 `queryDigest` preimage/algorithm. The digest projection excludes personal UID as well as dataVersion, operation, input, view, refreshCollection, share state, search, sort, and pagination; the later personal result-cache key adds the independently owned collection digest.
- Define a stable error envelope whose code and field-error codes, rather than localized message text, are the client logic boundary.
- Add the v1 `/ranking#q=v1.<payload>` and `/co-star#q=v1.<payload>` share format, including a closed `0 people = empty`, `1 = partners`, `2–10 = co-star analysis` topology whose selected identities are exactly the applicable operation input, plus canonical serialization, size/version checks, one-time replay rules, and strict negative vectors.
- Add language-neutral positive, negative, and normalization goldens plus locked, development-only schema/OpenAPI validation tooling and generation-feasibility checks for Go and TypeScript.
- **BREAKING** relative to prototype evidence: raw numeric/string positions, `isGlobal`, `enabled` range/tag wrappers, substring tag matching, and the prototype `?mode=` / `?result=series` URL state are not accepted by the formal wire. These are `INTENTIONAL_DELTA` corrections required by PRODUCT.md and the accepted data decisions.

Externally visible behavior is classified as follows:

- `PRESERVE_ORACLE`: ordered multi-position selection, ranking multi-position AND semantics, co-star per-position candidate grouping, collection statuses 2/3/4/5, NSFW/series options, range concepts, positive group-AND/group-OR tags, negative group-OR/group-AND tags, search/sort/page controls, and retaining the last successful Applied Query while Draft changes remain unapplied. Evidence: oracle `644b7748674e553f863d0ffd61d029f86fdc0717`.
- `INTENTIONAL_DELTA`: string subject types, `staff:*` / `cast:*` PositionKey namespaces, complete personal/global structural isolation, exact normalized tags, omitted inactive filters, 5/10/20 page sizes, canonical paths, and rejection of prototype-only fields/syntax. Authorities: PRODUCT.md “Query Application Contract”, `DR-DATA-TAG-001`, `DR-DATA-GLOBAL-001`, `DR-DATA-POSITION-001`, `DR-DATA-SUBJECT-TYPE-001`, `DR-DATA-SORT-001`, and the formal development master plan.
- `NEW_CAPABILITY`: formal error envelope, versioned self-contained share fragment v1, language-neutral contract vectors, and Go/TypeScript generation-feasibility evidence. Authorities: PRODUCT.md “Share query contract” and “Operation result boundaries”, plus the backend development guide sections 7, 8, and 11.

## Capabilities

### New Capabilities

- `contracts-query-wire`: Versioned strict schemas, OpenAPI components, normalized goldens, error envelope, share fragment v1, and cross-language generation inputs for shared query transport.

### Modified Capabilities

- None.

## Impact

| Field | Declaration |
|---|---|
| Status | investigated: complete; specified: contract/tooling semantics, empty-cache-parent correction, and small-correction governance approved after independent zero-finding reviews; implemented: tasks 1.1–5.2 complete, including the original six-leaf cleanup and accepted exact empty-parent correction; verified: owner schema/vector/codegen/cold-cache/cleanup/OpenSpec/envelope gates plus main and independent read-only candidate acceptance passed, with all generated/cache/temp/link/empty-directory residue absent; committed: planning/control history through `dcadeb0cb3092583b4c368767a0777cecc719596` only, Wave 1A product/finalization protocol 5.3–5.4 not committed yet; pushed: no; released: no; deployed: no |
| Owner | Contracts spec/apply/finalization roles remain delegated; main-agent product mutation is allowed only by the exact user-approved small-correction exception below |
| Writable paths | Planning: only `openspec/changes/define-shared-query-wire/**`. Apply: only `contracts/openapi/**`, `contracts/schemas/query/**`, `contracts/goldens/query/**`, exact root `.gitignore`, and own task-checkbox markers. Root `.gitignore` is a narrow Query tool-state boundary, not general product ownership |
| Read-only protected inputs | `PRODUCT.md`; `DESIGN.md`; `openspec/config.yaml`; `openspec/specs/contracts-rewrite-baseline/spec.md`; `tmp-formal-development/formal-development-master-plan.md`; `tmp-formal-development/data-logic-implementation-guide.md`; `tmp-formal-development/backend-development-implementation-guide.md`; `tmp-formal-development/decisions/prototype-data-logic-audit.md`; `.impeccable/**`; oracle commit `644b7748674e553f863d0ffd61d029f86fdc0717`, especially `frontend/src/workbench/types.ts`, `frontend/src/workbench/components/QueryWorkspace.vue`, `frontend/src/workbench/composables/useWorkbench.ts`, and the two `frontend/public/workbench-data/*.json` fixtures; externally created untracked `.vscode/settings.json` at SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3` |
| Deletion complement | Every tracked path outside the apply roots except the explicitly authorized rewrite of root `.gitignore`. No existing contract path may be deleted/moved, and no other root/framework/editor/sibling path may be modified, hidden, or ignored. The exact sealed untracked `.vscode/settings.json` is tolerated protected foreign state, not writable output |
| Mutable refs | Accepted checkpoints are initial `c7f868e2861e8fea250f033c27538ecf793bacad`, Archive correction `ad5bf1a0fbca06aba1f7d2cff5be66e1d726701c`, Query correction `52af379d48d7c29a479b59a4670ae07d73b1c0fb`, and editor-state amendment `dcadeb0cb3092583b4c368767a0777cecc719596`. Product writes remain permitted only inside the exact apply Writable paths; within OpenSpec control state, an apply owner may change only its own task-checkbox markers. Refs and index remain immutable. After both candidates are independently accepted, one finalization subagent alone may create the approved combined Wave 1A phase commit; no apply owner may stage or commit |
| Consumes | Completed `establish-formal-rewrite-baseline`; the authority and oracle inputs listed above; read-only npm registry and Go module proxy metadata/artifacts only for locked development tooling |
| Produces | `contracts/openapi/openapi.yaml`; v1 JSON Schemas under `contracts/schemas/query/**`; positive, negative, normalized/digest, limit-evidence, and code-generation-feasibility records under `contracts/goldens/query/**`; exactly three committed Node tooling files there (`package.json`, `package-lock.json`, `verify.mjs`); and root `.gitignore` narrowed from broad language-wide patterns to the exact Query transient paths while preserving macOS/environment rules |
| Dependencies | `establish-formal-rewrite-baseline` only |
| Deliverables | Personal/global query union; PositionKey; collection/status/range/tag normalization; cycle-free canonical queryDigest; named shared operation input/view components; error envelope; share fragment v1; versioning rules; strict unknown-field and failure cases; language-neutral goldens; Go and TypeScript generation-feasibility evidence |
| Acceptance | OpenSpec strict validation; JSON/YAML parse; JSON Schema draft 2020-12 strict compilation; OpenAPI 3.1 recommended-rules lint; every positive/negative/normalization/digest vector produces the declared outcome; repeated normalization, digest, and share encoding are byte-stable; `openapi-typescript@7.13.0` reads the authoritative endpoint-free source; the repo verifier proves its reference graph is a sibling-free DAG with no dynamic/recursive/anchor semantics, creates two exact codegen-only source projections that differ from authority only by deletion of seven root `$id` and seven root `$schema` keys, and independently expands the 17 public entries; locked Redocly runs with telemetry off, explicit empty config, clean environment, and exact network-denial sandbox to produce byte-identical fully dereferenced JSON bundles whose public schemas and response schemas deep-equal the applicable independent expansions while non-schema response fields remain exact; exact `oapi-codegen/v2@v2.8.0 -generate models,skip-prune` generates deterministic non-header Go models from projection A only; all 17 public component names appear in the Go declaration inventory and temporary Go syntax/format/compile smoke passes; exact root `.gitignore` bytes ignore only the six Query transient paths plus preserved macOS/environment patterns, leave every non-preserved product/lock/test/OpenSpec/editor/foreign escape visible, and are verified with explicit positive and negative probes, while every ignored transient is nevertheless physically absent from the candidate; paired-worktree path/index gates pass |
| Non-goals | No endpoint path/handler, catalog implementation, store/cache implementation, runtime cache lookup, collection access, statistics, result DTO, UI/store/request adapter, share service/short code/server session, runtime Go/TS consumer test, or Archive contract |
| Operations deferred | No nginx, systemd, production Compose, timers, deployment/release workflows, secrets, host changes, production activation, registry push, monitoring rollout, or migration |
| Stop/rollback conditions | Stop before mutation on branch/accepted-checkpoint mismatch, non-empty index, dirty paths outside the exact paired apply envelope, exact read-only Wave 1B planning allowance, and unchanged sealed `.vscode/settings.json`, any editor-state drift, missing main-agent approval, dependency not accepted, protected-input drift, schema/tool disagreement, network/tool lock drift, any write outside this change's apply roots/own task-checkbox markers, or any P0/P1 review finding. Preserve evidence and correct only this change's exact paths with explicit review; never use reset-hard, checkout rollback, git clean, recursive broad deletion, or history rewriting |

Acceptance additionally requires a closed Go-stderr policy and a fail-closed cleanup proof. Stable generated bytes, hashes, declarations, and stdout remain exact. Policy `go-download-progress-v1` applies only to the final status-zero candidate-success generation/replay/compile evidence; historical rejected and corrected recovery attempts remain in a separate history namespace and neither satisfy nor fail candidate admission. Candidate generation/compile stderr may be empty or contain only complete LF-terminated `go: downloading <module> <version>` lines whose exact `module@version` pair belongs to the manifest's lexically sorted union allowlist for the pinned generator tool graph and temporary compile graph. `gofmt` stderr remains empty, and every warning, denial, control character, partial line, unlisted pair, or other text fails. The stable manifest records the policy version and exact pair allowlist rather than cache-dependent stderr byte counts; the handoff records each candidate-success child's actual bytes and policy result. Before the one real cleanup, a synthetic safety test must prove read-only-directory removal, internal-link no-follow behavior, root/ancestor-link rejection, preserved outside-target sentinel bytes, and zero test residue. Real cleanup may remove only the six exact allowlisted roots, with per-segment `lstat` and containment checks, directory-only owner-permission repair, bounded retry, post-removal `ENOENT` proof, and deterministic sorted `removed` versus `alreadyAbsent` evidence.

The first live cleanup completed once and correctly removed the six exact leaves, but left their now-empty, unignored container `contracts/goldens/query/.cache`; therefore the candidate is not accepted. One correction apply may change only `verify.mjs`, exactly `acceptanceEvidence.projectionTool.verifier.{bytes,sha256}` in `manifest.json`, this change's own task checkbox, and that exact empty directory. The other 22 Query product files must remain byte-identical. The `verify.mjs` diff may touch only generated-root/empty-parent cleanup helpers, cleanup-safety cases/lifecycle, cleanup dispatch, and cleanup evidence output; schema, vector, normalization, Unicode, share, codegen, Go-stderr admission, and every other verifier path remain byte-identical.

The corrected verifier must retain the six-leaf allowlist, then prune `.cache` only after all four approved `.cache/*` leaves are `ENOENT`, a no-follow `lstat` chain proves the exact parent is a real in-bound directory, and `readdir` proves it has zero entries. It performs one initial non-recursive exact-target removal plus no more than five retries. Before every retry it repeats exact `lstat` and zero-entry `readdir`; only `EBUSY`, `EMFILE`, `ENFILE`, or `EPERM` from the removal call may wait exactly 100 ms and retry. `ENOTEMPTY`, an `ENOENT` race, link/type/entry drift, any other error, retry exhaustion, or a surviving parent fails immediately without recursive deletion. An already-missing parent at initial inspection is `alreadyAbsent`.

Its cleanup-safety mode must exercise empty-parent removal and non-empty-parent rejection before the one narrowly authorized correction cleanup. Safety preflight requires exact `.tmp` `ENOENT`; that mode alone creates `.tmp` as its synthetic container, removes all fixtures, then non-recursively removes the exact `.tmp` it created and proves `lstat -> ENOENT`. The correction cleanup must therefore report all six leaves `alreadyAbsent` and only `.cache` in `emptyParents.removed`. It may not rerun install, schema/vector/codegen/generator/compile/full-verifier work or recreate any other product/cache root. Artifact status text remains main-agent-owned OpenSpec.

This change does not write another repository or mutate external services, hosts, production state, releases, tags, pull requests, or deployments. Dependency downloads are read-only external inputs and their exact versions/integrity are locked in-repository. Any external push, pull request, tag, release, deployment, host mutation, or production activation requires a separate later authorization.

Apply is blocked until proposal, specs, design, and tasks are complete, `openspec validate --all --strict` passes, and the main agent explicitly records approval.

Small-correction exception (user-approved 2026-07-24): after the applicable OpenSpec is already exact, strict-valid, independently reviewed with zero P0/P1 findings, and no implementation owner is active, the main agent MAY directly implement a mechanical correction affecting at most two exact product/test files plus its own task marker. It SHALL add no capability or public behavior, dependency, contract/schema, architecture, external/remote state, or broad/unbounded destructive cleanup; SHALL remain inside approved writable paths; and SHALL run the same acceptance. A safety-sensitive correction additionally requires independent zero-P0/P1 candidate review. Every substantive development block and every stage/archive/commit/ref mutation remains delegated. Query task 4.6 is the first application of this exception.

Wave 1A apply is paired with `define-archive-manifest-contract` from one approved planning/correction chain and an empty index. The two implementation agents may work concurrently only in their disjoint declared paths, must snapshot and never write the sibling's task/apply paths, and may tolerate outside them the exact untracked non-symlink `.vscode/settings.json` at SHA-256 `048f53e6ca01ac583b48784cd2f6f7d248e0534849955b144e75f017f73188a3` plus spec-only work under exactly `openspec/changes/bootstrap-backend-runtime/**`, `openspec/changes/bootstrap-updater-runtime/**`, and `openspec/changes/bootstrap-frontend-foundation/**`. Those three directories are read-only foreign planning envelopes for Query, may contain only OpenSpec artifacts, and may never be used as Query input, targeted by its commands, staged, committed, archived, or interpreted as dependency completion. Any other drift stops work without repair. Query keeps checks path-scoped and stops as an unstaged candidate. After both Wave 1A candidates pass main-agent acceptance, one finalization subagent alone stages the exact Wave 1A paths, archives both changes, validates the combined result, and stops for final main-agent read-only acceptance; only then may it create the single commit with exact subject `feat(contracts): establish wave 1 shared contracts`.

During apply, a separately authorized subagent in `/Users/luca/dev/bangumi-collection-go` started Go 1.26 before the Query owner started any ordinary Go process. A new 16,384-byte Go 1.26 telemetry counter appeared at the matching time, but persistent editor-owned `gopls`/`vscgo` processes can also update the same global telemetry directory. The main agent interrupted the external collection-client owner; it remains stopped until Query's Go checks finish. No whole-directory snapshot is an authorization or acceptance invariant, and the change does not claim exclusive attribution for global counter-byte drift. Instead, every Query command capable of starting Go must execute inside the reviewed macOS `sandbox-exec` profile that denies `file-write*` beneath the canonical telemetry directory. Missing/bypassed/failed wrapping is fail-closed. This correction does not authorize telemetry writes, deletion, interpretation, mode changes, user-process termination, or a second discovery probe.
