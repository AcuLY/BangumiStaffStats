## Task Boundary

| Boundary | Declaration |
|---|---|
| Status | investigated: complete; specified: approved; planning/main review: passed; implemented: complete; Contracts verification: passed; downstream Go/Python runtime verification: pending in their owning changes; committed/pushed/released/deployed: no |
| Owner | One Contracts owner implements contract evidence; main agent records dependency ordering, reviews, and accepts. Backend/updater owners adapt only in their existing changes after this exits. |
| Writable paths | Contracts group: `contracts/schemas/archive/archive-manifest.schema.json`, `contracts/schemas/archive/README.md`, `contracts/schemas/archive/tooling/build_sqlite_fixtures.py`, `contracts/schemas/archive/tooling/verify.mjs`, `contracts/goldens/archive/index.json`, new `contracts/goldens/archive/vectors/manifest-string-semantics.json`, and this file's task markers. Main group: `tmp-formal-development/formal-development-master-plan.md` and this file's task markers. Disposable verification: `contracts/schemas/archive/.cache/**`, `contracts/schemas/archive/.tmp/**`, and `contracts/schemas/archive/tooling/node_modules/**`, all absent at exit. |
| Read-only protected inputs | Higher authorities, all root specs, archived/completed/other active changes, the existing 31 golden files, every other Contracts path including tooling package/lock, all backend/updater/frontend code/tests, refs/remotes, external repositories, hosts, and production. |
| Deletion complement | Remove only canonically validated disposable roots at exit and, on failed unpublished apply, the new vector candidate. Preserve every accepted path and all prior 31 golden bytes. |
| Mutable refs | None; no stage, commit, archive, branch/ref update, or push during apply. |
| Consumes | Accepted/exited subject correction, corrected 31-file corpus, authoritative manifest schema/tooling, approved artifacts, and read-only Node/Go divergence evidence. |
| Produces | Exact generated-time and Unicode-scalar rules, one indexed vector, Node/Python/isolated-Go evidence, and consumer/producer dependency gates. |
| Dependencies | Accepted Archive contract and completed/exited `correct-archive-subject-semantics`; separately reconciled consumer/producer plans that block their final acceptance on this change. |
| Deliverables | Exact schema/docs/tooling/vector/index edits, 25 string cases plus one raw-byte recipe, deterministic three-language probes, and master-DAG row/count/edges. |
| Acceptance | Prior 31 golden bytes unchanged; exact 32-path index; all timestamp, scalar, surrogate, raw UTF-8, and URL-bound outcomes agree; builder/verifier/OpenSpec/diff/inventory/residue gates pass; no browser gate because no frontend is touched. |
| Non-goals | Runtime adaptation, new dependency/version, root-spec sync/archive, URL origin/normalization policy, product/query/frontend work, full Archive, or broad cleanup. |
| Operations deferred | Current pointer, acquisition, production paths/data, activation, migration, scheduling, locking, retention, restart, rollback operation, release, deployment, hosts, and secrets. |
| Stop/rollback conditions | Stop on branch/HEAD/index/dirty scope, correction, baseline, dependency, ownership, path, deterministic, or verification mismatch. Revert only owned unstaged bytes; never use reset-hard, checkout rollback, git clean, `git add -A`, broad deletion, or external writes. |

## 1. Contracts implementation

- [x] 1.1 Preflight `git branch --show-current`, `git rev-parse HEAD`, `git status --porcelain=v1`, `git diff --name-only`, and `git diff --cached --name-only`; require the reviewed four artifacts, empty index, no pre-existing write-path drift, accepted/exited correction, exact 31-file/hash baseline, no formal/public/activated/released/deployed v1, separately recorded consumer/producer acceptance blocks, and no overlapping owner; stop before mutation on mismatch while preserving unrelated dirty paths.
- [x] 1.2 Update only `archive-manifest.schema.json` and `README.md`: retain the exact UTC lexical form and manifest version 1, attach the no-dependency named formats `bgmss-utc-generated-at-v1` and `bgmss-unicode-scalar-url-v1` to their schema properties, and specify year `0001..9999`, 1900/2000 leap behavior, hour 24/minute 60/second 60 rejection, fraction 0/1/6/7 boundaries, legal surrogate-pair decoding as one scalar, isolated high/low-surrogate rejection, and fatal raw UTF-8 first.
- [x] 1.3 Update only `build_sqlite_fixtures.py` to deterministically emit the single exact vector with the 25 approved `jsonStringLiteral` cases and one raw malformed-UTF-8 recipe that retains the `archiveAssetUrl` JSON string delimiters while replacing exactly its payload with bytes `C3 28`; recompute scalar/UTF-8-byte facts and add exactly its one digest entry to `index.json`, requiring 32 indexed regular non-symlink paths while preserving all prior 31 paths and bytes.
- [x] 1.4 Update only `verify.mjs`: register both named Ajv formats before strict schema compilation and enable their assertions; implement explicit timestamp arithmetic and the scalar guard, reject isolated surrogate escapes before replacement, execute every vector case against the corrected minimal manifest, and run one Python probe plus one directly sandboxed isolated-Go probe from the same tracked vector, with Go validating raw JSON escapes before `encoding/json`; materialize the raw-byte recipe only below `.tmp`; do not change `package.json`, `package-lock.json`, or any runtime source.
- [x] 1.5 Run `python3 contracts/schemas/archive/tooling/build_sqlite_fixtures.py --check` twice from clean disposable roots and require identical reports/directory seals; then from `contracts/schemas/archive/tooling` run the existing approved cache/telemetry-sandbox setup with `npm ci --ignore-scripts` and `npm run verify`, proving all 25 string cases plus the raw-byte recipe report exact `VALID`/`MANIFEST_SCHEMA_INVALID`, year 0000 and 1900 fail, year 2000 passes, 24/60/60 and fraction 0/7 fail, fraction 1/6 pass, the 2048-scalar/2051-byte URL passes, isolated surrogates fail without U+FFFD, and no dependency/lock drift occurs.
- [x] 1.6 Remove only the three canonically contained disposable roots through the reviewed exact cleanup path; run `git diff --check`, physical/index/hash equality, prior-31-byte equality, no-current/no-generated/no-residue, exact writable/protected-path diff, and `openspec validate harden-archive-manifest-string-semantics --strict`; report investigated/implemented/verified states without staging.

## 2. Main-agent dependency record and acceptance

- [x] 2.1 Before Contracts apply, preflight branch, HEAD, empty index, status, reviewed artifacts, exact master-plan preimage, no other master-plan writer, and separately amended consumer/producer plans that explicitly block final acceptance on this hardening; all checks passed before the dependency-record edits.
- [x] 2.2 Update only `tmp-formal-development/formal-development-master-plan.md`: insert `harden-archive-manifest-string-semantics` directly after the completed correction, change the main-repository count 28→29, make consumer and producer directly depend on it, preserve query-result's transitive dependency and the exact Wave 5 direct-19 list, and make no runtime edit.
- [x] 2.3 Independently review the full owned diff, all timestamp/scalar/surrogate outcomes, unchanged prior 31 golden bytes, exact 32-path index, no-dependency decision, isolated-versus-runtime proof distinction, DAG edges/count, protected/external state, and zero residue; rerun `python3 contracts/schemas/archive/tooling/build_sqlite_fixtures.py --check`, the approved `npm run verify`, `openspec validate harden-archive-manifest-string-semantics --strict`, `openspec validate --all --strict` when no unrelated draft prevents it, `openspec doctor`, and `git diff --check`. Browser acceptance is not applicable.
- [x] 2.4 Record accepted Contracts status and hand the exact indexed vector to the existing consumer/producer owners; keep both final acceptances blocked until their real runtime boundaries pass it. Leave root-spec sync, archive, stage, commit, push, release, deploy, activation, and all operations to separately authorized lifecycle work.
