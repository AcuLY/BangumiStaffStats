# Verification evidence

## Specification gate

Primary reviewed the six capability deltas against approved scope and live source: single-type all is explicit, legacy concrete selection/digest remains unchanged, wish canonical identity is common to five labels, exact staff/cast evidence and per-Subject union remain backend-owned, one-shot entry is separate from general sharing and operation scopes, and deployment remains in the separate authorized change. Conflicting PRODUCT/DESIGN and stale AGENTS routing prose were reconciled without live route writes.

`OPENSPEC_TELEMETRY=0 npm exec --yes --package=@fission-ai/openspec@1.6.0 -- openspec validate add-unrestricted-person-entry --strict` → exit 0, `Change 'add-unrestricted-person-entry' is valid`.

Deployment change strict validation also exit 0; `git diff --check` exit 0. This is the specification gate, not feature acceptance.

## Baseline

Pinned Node v24.18.0 / npm 11.16.0 frontend `npm ci --ignore-scripts --no-audit --no-fund && npm test -- --maxWorkers=2`: exit 0, 48 test files / 583 tests passed. npm reported an existing deprecated glob warning; no dependency upgrade was made.

Pinned backend `./scripts/check.sh`: exit 1 before product tests due to Tencent mirror ECONNRESET downloading yargs-parser-21.1.1.tgz. No backend pass is claimed. Exact-integrity dependency recovery or official CI is required.

## Implementation / integration

Standalone userscript slice implemented by primary while contracts authority and dependency-cache recovery run in non-overlapping scopes. Root `bangumi_plugin.js` and `frontend/tests/app/bangumi-plugin.test.ts` are owned by primary; frontend integration/artifact publication remain pending.

TDD command from frontend: `bash /root/.hermes/workspace/bangumi-staff-stats/toolchains/with-pinned-tools.sh npm test -- --maxWorkers=2 tests/app/bangumi-plugin.test.ts`.

- Initial test harness import needed a local path correction before meaningful execution (fileURLToPath encountered Vitest HTTP module URL).
- RED after correcting harness, before implementation: 10 failed / 6 passed, missing userscript entry/metadata as expected.
- GREEN after implementation: exit 0, 1 file / 16 tests passed; git diff --check exit 0.
- Cases: all three Bangumi domains, fixed destination, four secondary types, current navigation identity, body-link rejection, conflicting/external identity fail-closed, UID encoding, activation-time logout, idempotence, Escape/outside-close/focus, malformed person paths and privilege-free metadata.
- Real anonymous Bangumi `/person/6447` DOM inspected: `#headerNeue2 .idBadgerNeue`, `#dock` and `#headerSubject` landmarks exist. First follow-up failed because the active browser context had moved to about:blank; explicit navigation plus injection in one call succeeded. Installed=true, all five links had no href while logged out, primary activation displayed the exact login message, secondary disclosure opened correctly.
- Actual screenshot exposed a native header alignment/button-style mismatch; fixed the root to the existing 1200px centered header content line and matched native light/dark button tokens. Re-run of16 script tests and diff check passed. At widths1280/390 the real page had no viewport overflow; layout JSON: `/root/.hermes/workspace/bangumi-staff-stats/userscript-browser-layout.json`. Desktop menu geometry verified; the second loop toggled mobile menu closed, so open-mobile-menu acceptance remains pending.
- Chinese glyphs in the browser environment render as boxes (also on the untouched Bangumi page); DOM text is correct but visual typography acceptance is incomplete. No authenticated end-to-end query is claimed.
- `npm run typecheck` initially exposed untyped existing jsdom module import in the new test. Confined its minimal typed boundary to the test via createRequire without dependency additions; re-run16 tests plus full frontend typecheck exited0.

## Standalone userscript review correction

Independent spec reviewer identified P2: the required login notice was missing initially and disappeared on opening the secondary menu. Primary verified against the first requirement and reproduced with three new regressions: RED 3 failed / 16 passed. Minimal refreshLinks correction made the notice depend solely on unavailable identity, not activation.

Real browser then exposed the notice underneath the open dropdown (notice y145–163, dropdown y141–327); assertion failed with overlap=true. Added a DOM-order guard (RED 1 failed / 18 passed), placed the notice before the action row and moved its spacing below. Final pinned `npm test -- --maxWorkers=2 tests/app/bangumi-plugin.test.ts && npm run typecheck` → exit0, 19/19 tests, clean typecheck. Tests also cover retaining the explanation through menu/context-menu interaction and clearing it after a known navigation identity appears.

Real anonymous `https://bgm.tv/person/6447` rerun: 1280px/390px × light/dark, four distinct states verified from saved JSON; initial and open-menu message exact, all five hrefs absent, no menu/notice overlap and no viewport overflow. Evidence `/root/.hermes/workspace/bangumi-staff-stats/userscript-login-notice-browser.json`. This closes the earlier open-mobile-menu geometry gap, not the authenticated flow or typography acceptance. Separate screenshot calls initially hit a fresh about:blank context; explicit navigation, injection and screenshot in one batch succeeded. The mobile screenshot confirms the notice sits visibly above the open menu, but Chinese glyphs remain boxes throughout the untouched site as well; host font matching falls back to DejaVu Sans. CJK typography acceptance remains incomplete. Independent spec re-review approved the first requirement with a fresh19/19 targeted run and no remaining spec gap; code-quality review found no Critical/Important issue and two Minor findings. Primary fixed ArrowDown state refresh after RED1 failed/19 passed, and made the idempotence test actually execute the second installation callback while asserting one control and one style. Final standalone target test20/20 and diff check pass. Independent final quality re-review APPROVED both fixes with no new finding. Its read-only JSDOM checks exercised initialization, login/logout refresh, visibility/focus/default prevention, and in-memory removal of either fix failed. Standalone spec and quality reviews are closed; the app typecheck failure below remains distinct.

## Contracts authority handoff

Primary reread the actual changed shared schema, schema tests and normalizer diff. Independent rerun with pinned Node and existing frontend Ajv (no fetch/install):

`bash /root/.hermes/workspace/bangumi-staff-stats/toolchains/with-pinned-tools.sh node --import /root/.hermes/workspace/bangumi-staff-stats/toolchains/resolve-existing-ajv.mjs --test contracts/goldens/query/test-unrestricted.mjs` → exit 0, 12/12 tests passed. Covers shared/effective/projection closure, all+empty exclusivity, malformed scope, wish order, five request references and operation-empty compatibility. Local resolver initially recursed because require.resolve re-entered its hook; resolving dependency URLs before registering the hook corrected the harness without changing product code.

Compared HEAD and working-tree fixture JSON within one local process, before output formatting: all 42 existing fixtures unchanged (6 positive, 35 negative, 1 digest-exclusion). Current sections contain 16 positive, 39 negative, 1 digest-exclusion. Existing digest/preimage data were not changed. Do not compare terminal-rendered encoded strings; output redaction can alter representation.

Implementer reported authority-only verifier and five API verifiers passed; these have not been independently rerun by primary because their local dependencies are absent. They are not full consumer acceptance. Independent contracts spec review approved tasks2.1/2.2 with no actionable violations. It separately reran12/12 schema tests, confirmed all42 old fixture cases unchanged, checked preimage/hash consistency and traced OpenAPI/request references. Independent code-quality review approved proceeding to generated consumers with no Critical/Important finding. Its only Minor finding is the audit predicate’s order-sensitive JSON.stringify comparison at verify-current.mjs:1755–1756; current schemas pass and strictness is preserved, so structural-equality hardening is deferred until the full verifier can be exercised. This is not whole-feature merge approval.

Earlier separate frontend rerun: pinned `npm run typecheck && npm test -- --maxWorkers=2 tests/app/bangumi-plugin.test.ts` → exit 0, typecheck clean and 16/16 script tests passed. The later userscript correction above supersedes this with19/19 tests and clean typecheck. No full-feature frontend pass is inferred.

## Generated-consumer preflight

Pinned frontend `npm run check:query-wire` used existing dependencies and executed @hey-api/openapi-ts0.99.0 successfully in its disposable check directory; exit1 is the expected stale-consumer guard: line65 expected `};`, actual `positionScope?: 'all';`. No tracked generated files were written. After authority spec/quality approval, all six frontend generators (query, rankings, candidates, person-detail, partners, co-star) ran with existing dependencies, followed by their six drift checks:12/12 commands exit0. Evidence `/root/.hermes/workspace/bangumi-staff-stats/generated-consumer-results.json` was reloaded and checked for all12 distinct component/mode pairs. Generated files were produced only by existing scripts;11 generated files changed. `git diff --check` passed.

The post-generation `npm run typecheck` now exits2 at `src/features/query/model.ts(842,13)`: the old label map lacks wish. This is the expected not-yet-implemented frontend consumer, not a green overall frontend state. Earlier standalone-script/typecheck successes predate generated wire updates.

Started the ordinary, already specified backend query generator via the pinned wrapper (`./scripts/generate-query-wire.sh --write`), without changing its script, lockfiles, registry, proxy or adding a cache-repair shim. The ordinary generator exited1 after181s before producing Go models: Tencent yargs-parser-21.1.1.tgz TLS connection reset (ECONNRESET), same dependency blocker as baseline. No Go generation success or backend pass is claimed. Further Go-generator retries and recovery were stopped at that point; the explicit renewed authorization and verified recovery below supersede that pause. Remaining Go generators, manifests and full consumer gates are pending.

## Dependency blocker and acceptance boundary

Cache-recovery delegate received a fresh approval timeout on a bounded lockfile/network diagnostic and stopped. No recovery writes, cache seeding, downloads or backend rerun occurred. Do not retry that denied operation or repackage it through another tool/agent without renewed authorization. Source inspection established that backend check.sh deletes its disposable .cache/.tmp at entry and exit, so pre-seeding that cache would not survive the entry cleanup and failure logs must be retained outside disposable roots. Official CI on the exact candidate remains the separately documented acceptance alternative; no CI pass is claimed.

The earlier renewed-authorization question timed out and was not permission. The user subsequently requested a fresh authorization and explicitly replied “允许” after the same bounded scope was restated. Dependency recovery is now authorized and verified; the old pause no longer applies. No broader registry/proxy/lockfile or production mutation was included in this recovery.

### Verified dependency recovery

- Read seven existing contracts/goldens lockfiles and deduplicated their 80 package references into 38 exact tarball URLs. Fetched the same versions from registry.npmjs.org, verified every tarball against its original lockfile SRI before writing it, and rechecked all seven lockfile SHA256 values unchanged. Saved manifest was reloaded and its unique count checked:38/38,9250417bytes.
- Recovery files live only under `/root/.hermes/workspace/bangumi-staff-stats/toolchains/cache-recovery/`; content-addressed cache was also populated under existing `backend/.cache/npm`. No repository lockfiles, global registry/proxy settings, credentials or production files changed.
- `fetch-verified.py` → exit0; pinned `seed-cache.mjs` → exit0. The unchanged backend query generator with process-local `npm_config_offline=true` successfully installed38 packages in2s, then hit the expected stale acceptanceEvidence guard: generated bundle748818bytes versus recorded693640bytes. That was pending authoritative manifest regeneration (subsequently completed through the existing refresh path below), not a remaining network failure; no assertion was relaxed and no generated evidence was hand-edited.
- `run-with-verified-cache.sh` plus `bin/npm` are isolated, pinned helpers: before each npmci, seed only an explicitly allowed cache from reverified tarballs, then execute the real pinned npm with offline mode. This places cache seeding after any invoking script cleanup without editing project scripts. A separate fresh `offline-smoke` install using the isolated tool cache added38 packages offline with exit0; evidence `toolchains/cache-recovery/offline-smoke-result.json`.
- Query verifier dependencies are now installed from the same verified cache. Primary independently ran pinned `node contracts/goldens/query/verify-current.mjs --authority-only` → exit0:6 strict schemas,127 golden cases,95370 pinned NFKC assertions. This supersedes the earlier missing-canonicalize limitation, but does not claim generated consumer acceptance.
- Generated-consumer owner reported supported authority refresh/replay, six backend write/check pairs and six frontend drift checks passed. Primary inspected the actual changed six Go outputs plus query manifest, unchanged HEAD/lockfiles/go.sum and clean diff hygiene; independent generated-slice spec review returned SPEC COMPLIANT for task2.3. Reviewer replayed all six backend checks, compared six frontend families generated into disposable outputs byte-for-byte, ran full query acceptance (127 goldens/54600 cross-validations), fresh wire Go tests and vet, and confirmed unchanged OpenAPI bytes match the existing packaging pin. Primary independently reran `node --test contracts/artifacts/test/*.test.mjs` under the pinned wrapper: exit0,51/51 tests passed; query manifest diff and diff hygiene were also checked. Not every reported aggregate has been independently replayed by primary. Generated-slice quality review returned APPROVED FOR THIS SLICE with no Critical/Important/Minor finding introduced: additive optional serialization, unchanged legacy generated logic, five schema families’ strictness, and acceptance-evidence-only manifest updates checked. Primary then independently verified actual Go/frontend bytes and SHA256 against the manifest and confirmed other manifest sections unchanged. Task2.3 is complete; task2.4 and business integration remain unaccepted. The final redundant report-read command received a fresh gateway denial; neither that command nor the denied cumulative report is being retried through another route.

### Resolved catalog/query-domain metadata blocker

Primary independently reproduced query-domain verifier exit1 at catalog-synthetic-staff-set-v1 authority hash. Read-only Git-blob comparison found the catalog mismatch already exists at HEAD; the other four mismatches reference deliberately changed query schema/cases/manifest. Further unchanged catalog verifier run also exits1 on config/staff-sets-empty-v1.json hash drift. The catalog-index audit checks all five entries: exactly three stale digests (two configs and one quality sentinel) match their CRLF variants, while actual checked-in bytes are LF. The synthetic config source is unchanged since its initial commit. Evidence `/root/.hermes/workspace/bangumi-staff-stats/catalog-newline-audit.json`. The user then explicitly authorized the pending scope (“都允许”). Primary amended the exact owner inventory, passed strict validation, and computed the correction from unchanged Git-blob-identical source bytes: only three catalog index SHA256 fields, six query-domain authority references in manifest/verifier, and the verifier self-hash changed. All catalog configs, fixtures, quality data, catalog verification code and lockfiles remain untouched; query-domain verifier code differs only in hash literals. Primary reviewed the exact diff and reran catalog acceptance (five indexed files), query-domain acceptance (eight cases: seven success cases and one expected cancellation), existing closed-inventory and fatal-UTF8 negatives (both rejected as required), and artifact tests51/51. Evidence `metadata-repair-plan.json` and `metadata-repair-verification.json` outside the repository. Five API verifiers also independently passed76cases, recorded in `contracts-pre-backend-verification.json`. This closes the contracts pre-backend gate, not backend/frontend business or full-feature acceptance.

## Backend implementation checkpoint — not yet accepted

The backend implementation delegate timed out after600s without a summary. Primary recovered the actual dirty/untracked files and `/root/.hermes/workspace/bangumi-staff-stats/backend-feature-results.json`; timeout is not treated as success. Recorded RED/GREEN work covers optional all normalization, wishlist mapping/cache, actual-participation union, cast applicability/detail and explicit candidate/partner/co-star identity integration. Initial catalog-prerequisite and unsupported-test-identity errors were corrected before meaningful product regressions; a phase label containing GREEN but exit1 remains a failed attempt, not acceptance.

Primary inspected the eleven changed non-generated source/test files plus nine new files, formatted only those20 owned Go files with the pinned Go toolchain, and independently ran:

`env GOCACHE=$REPO/backend/.cache/go-build GOMODCACHE=$REPO/backend/.cache/go-mod GOPATH=$REPO/backend/.cache/go-path GOENV=off GOWORK=off bash /root/.hermes/workspace/bangumi-staff-stats/toolchains/with-pinned-tools.sh go test ./internal/query ./internal/statistics ./internal/publiccollection ./internal/runtimecache ./internal/ranking ./internal/persondetail ./internal/candidates ./internal/partners ./internal/costar ./internal/httpapi -count=1`

From backend this returned exit0 with all ten named packages passing. Evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-parent-checkpoint.json`. Existing tests plus narrow new cases are not proof of all downstream/all-type/HTTP requirements. Independent backend SPEC review (`deleg_a3349ecd`) returned NOT COMPLIANT: `OperationPositions` routes shared unrestricted requests through selectable/supported catalog identities, dropping hidden/unresolved factual staff in candidate/partner browsing and rejecting raw identity round-tripping in co-star and explicit detail. Default detail and the unrestricted ranking core preserve these credits. Primary verified the exact membership/replacement source chain against backend-query-result-set§48,55–61; no blanket relaxation of legacy catalog/capability guards is authorized. The reviewer independently reran eight focused packages with `-run 'Unrestricted|Wish'` successfully, demonstrating that passing narrow cases do not cover this integration gap. The scoped correction (`deleg_1ebd44ba`) introduced `query/operation_facts.go`, four service-level hidden-operation regressions, and the service/evaluator handoff changes. Four genuine service assertions first failed for missing people or rejected raw identities, then passed with actual people, work counts and exact raw credit evidence. The initial go-not-found exit127 was an environment failure and is not counted as RED; the corrected process explicitly included the existing Go bootstrap directory in PATH. Evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-hidden-operation-fix.json`. Factual membership is separate from legacy catalog-all; cloned per-request plan augmentation survives explicit narrowing via the original shared projection, and unrestricted same-person union is retained after narrowing. Primary inspected the new helper and actual diffs, then independently reran the same ten-package full test command with `PATH="/root/.g/go/bin:$PATH"` and `go version`: Go1.26.5 linux/amd64, all ten packages exit0. Diff hygiene and protected lockfile/go.sum/OpenAPI checks also exit0. Independent SPEC re-review (`deleg_7d700fcc`) closed the original hidden-staff omission but remains NOT COMPLIANT with two blockers: full factual loading/scanning bypasses mandatory detached executor admission, and no-credit book/music/real unrestricted candidate browsing fails instead of returning zero candidates. The admission issue is a direct violation of backend-bounded-query-cache§63–81, not merely an unmeasured performance concern.

Primary independently read the executor/timeout requirements and traced `LoadOperationPositions` before `ResultStore.GetOrCompute` admission. Primary also added `candidates/unrestricted_empty_test.go`, using the existing immutable minimal fixture whose retained staff rows are anime-only, and ran the new package test: all three book/music/real subcases compiled and failed with INTERNAL_ERROR at the expected assertion. Evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-empty-universe-red.json`. This was the pre-fix regression failure; the earlier ten-package green checkpoint did not include these newly added cases. The post-correction full-suite result is recorded below. The write tool's isolated-file undefined test-helper diagnostic was not a package compilation failure.

The scoped correction (`deleg_bfd75c81`) moved factual authority loading/scanning into all four existing admitted `GetOrBuild` callbacks. `LoadOperationAuthority` returns one loaded/prepared fact snapshot plus factual membership/browse sets; computation reuses that snapshot. Preflight performs bounded syntax/view checks; membership is revalidated from admitted facts, never granted by submitted keys. Explicitly unrestricted empty candidate results retain their internal mode, while ordinary empty/misaligned/current-position validation stays strict.

The new poisoned-fact Archive admission test first failed in all four services with preflight SQL/INTERNAL errors and zero executor rejections; the corrected test confirms two running, eight queued, four SERVER_BUSY rejections without factual reads. A four-service warm-result test switches to a poisoned same-version fixture and verifies cache hits without new executor starts. Implementer repeated the resource/detached regressions ten times successfully. Evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-admission-empty-fix.json`. Primary read the actual helper, service/parser/build diff and resource tests, then independently reran the full ten-package suite: Go1.26.5, exit0, including the new empty-universe, hidden-identity and resource cases. Runtimecache result/executor source, lockfiles, go.sum and OpenAPI protected checks and diff hygiene returned exit0. Race detection and full component check have not run. SPEC re-review (`deleg_7fbab3e3`) returned SPEC COMPLIANT for backend3.1–3.4: both admission/empty-universe blockers are closed, hidden/exact/legacy behavior and semantic cache identities remain sound, and fresh full/targeted tests passed. Its separate quality note identified a one-participant co-star unseen-key negative that could fail cardinality before testing membership; the subsequent quality correction strengthens this case. Code quality review (`deleg_9e710775`) returned FIXES REQUIRED: one Important omission of SQLite duration/error observation around the shared fact loader, plus three Minor findings (non-isolating co-star membership negative, missing outbound wishlist argument assertions, repeated per-credit identity formatting). It did not reopen the accepted admission or empty-universe corrections. Primary inspected the helper, trace API and existing adapter tests; Source.Fetch intentionally forwards the supplied status sequence including duplicates, so the wishlist test correction must preserve that behavior rather than change production deduplication. The narrow TDD correction (`deleg_d5fbd590`) is implemented in five files. Primary reread each changed file and inspected the actual RED/GREEN records: missing helper SQLite observations and stale success outcomes were reproduced, and the corrected helper/service tests pass for ordinary and unrestricted requests across all four services. The co-star negative now preserves two participants and asserts the exact membership error path; its temporary membership-bypass overlay failed as intended and was removed. Wishlist tests assert exact outbound enums, UID, call count and single/duplicate sequences across all five types. Numeric-position deduplication preserves filtering, lexical ordering and immutable facts; primary independently measured six allocations for both one credit and 4,096 identical-position credits. This is synthetic allocation evidence, not production latency acceptance. Primary fresh verification of ten related packages plus querytiming, focused verbose regressions and `go vet ./...` returned exit0; evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-quality-primary-verification.json`. Implementer evidence is `/root/.hermes/workspace/bangumi-staff-stats/backend-quality-fix.json`. Final independent quality re-review (`deleg_8738e814`) returned Backend-slice QUALITY APPROVED: all four findings closed, no new actionable issue or regression to the accepted SPEC slice. It independently reran all eleven related packages, targeted timing/identity/wish/allocation/admission/cache regressions, vet, diff hygiene and protected-source checks; all exit0. This is not full-component or release approval. Primary additionally ran architecture/app/observability package tests and `go vet ./...` with process-local GOPROXY=off, both exit0; evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-pre-gate-quality-checks.json`. These are additional checks, not the complete check.sh gate. Tasks3.1–3.4 previously completed their scoped implementation and reviews. The full component gate below exposed an instrumentation import-boundary violation; task3.4 is reopened for the service/helper correction and fresh review, while task3.5 remains pending full-component and explicit HTTP integration evidence.

Full-component preflight executed the unchanged exact-inventory block from `backend/scripts/check.sh` in isolation: it returned exit1, listing the new feature-owned Go files missing from its closed inventory. Evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-inventory-preflight.json` distinguishes this expected diagnostic failure from the successful probe harness. This was not a run of the complete component script. Primary reconciled the tasks/capability ownership declaration for this mechanical integration step, passed strict validation, and added exactly the 18 reviewed feature-owned Go paths to `expected_inventory`. All old entries remain; checker code outside that literal is byte-identical to HEAD. The real inventory block now returns exit0; an extra-file synthetic fixture still returns exit1 at the same closed-inventory guard. Bash syntax and diff checks pass; evidence `/root/.hermes/workspace/bangumi-staff-stats/backend-inventory-enrollment.json`. No dependency, budget, cleanup or other checker logic changed. The installed bootstrap is Go1.24.10; process-local auto-selection of the pinned Go1.26.5 remains required.

The first full existing backend `./scripts/check.sh` run (`proc_d9b29e6d44c7`) completed with exit1 after364.488s, not a timeout. It used ordinary Linux mode, Node24.18.0/npm11.16.0, Go1.26.5 auto-selection, two-process concurrency and the already authorized exact-SRI npm cache helper. Wire drift checks, initial httpapi/observability/app/publiccollection tests and HTTP fuzz passed. The first failure was `TestProductionPackageDependencies`: `internal/query -> internal/querytiming` is forbidden by the unchanged architecture guard. The new direct timing import in `query/operation_facts.go` caused this component-level regression; prior package/vet successes and the bounded quality approval did not exercise that guard on the corrected snapshot. Race, later component tests and builds were not reached. All protected before/after hashes match, diff hygiene passes and disposable roots were cleaned. Log/report remain at `/root/.hermes/workspace/bangumi-staff-stats/backend-component-gate.log` and `.json`. No checker relaxation is permitted. Correction `deleg_e9bdc4df` timed out at 600s without a final summary, but persisted all seven owned files and eight actual command records at `/root/.hermes/workspace/bangumi-staff-stats/backend-boundary-http-fix.json`. Primary inspected the actual helper/service/test changes and confirmed their hashes match the saved evidence. The helper now accepts a stdlib-only `func(time.Duration, error)` observer, invoked once around `LoadFactSet` before its error return; each service supplies existing querytiming observation inside its admitted callback. Plan preparation remains outside the timer. The unchanged architecture guard failed before correction and passed afterward. The added `TestUnrestrictedHTTPRealServicesPreserveWishAndLocalAbsence` exercises real ranking/detail services, Archive and shared runtime for personal/global unrestricted queries; only the external public-collection provider is substituted. It checks all-five statuses including wish, unrated works without zero ratings, distinct no-participation/entity-not-found HTTP errors, empty ranking, continued ranking access and another person's detail. Its initial failure was an incorrectly empty test catalog, not evidence of a product mutation; the fixture setup was corrected before the real test passed. Primary independently reran all twelve affected/architecture packages with `-count=1` and `go vet ./...` using Go1.26.5: exit0. Protected architecture/trace/runtimecache/dependency files and diff checks remain unchanged/clean. Bounded SPEC re-review `deleg_56912604` returned SPEC COMPLIANT after reading the actual seven-file correction and rerunning twelve packages plus the four exact architecture/observer/HTTP tests: exit0. It verified unchanged protected hashes and HEAD. Its two preliminary environment failures (offline toolchain verification, then inherited bootstrap GOROOT) were not product failures; successful offline runs used the already cached Go1.26.5 binary and unset GOROOT. Primary reproduced that the inherited GOROOT points to the bootstrap, verified `env -u GOROOT` selects Go1.26.5, and removed that inherited override only from the outer full-gate runner. Quality re-review `deleg_ac52d7c5` returned QUALITY APPROVED for this bounded correction with no actionable finding. Fresh twelve-package and four-focused-test runs, bounded vet, formatting and diff hygiene returned exit0; seven owned and72 protected hashes match. Primary repeated the hash/inventory preflight: no owned/protected drift, exactly18 added inventory entries and no other checker logic changes. Task3.4 is accepted again. The final-snapshot full existing backend gate (`proc_a4c8d1b6c0e6`) finished with exit0 after564.67s, not a timeout. Primary read the actual completed report and log: all seven generated wire drift checks, strict formatting/dependency stability, HTTP and statistics fuzz, architecture and all domain packages, twenty-repeat runtimecache/statistics suites, benchmarks, full `go test ./...`, `go test -race ./...`, vet, build, CGO-disabled tests/build, module verification, exact inventory/budget checks and final cleanup all passed. `backend-component-gate-r2.json` has `passed=true`, identical protected hashes, `diff_check_exit=0` and both disposable roots absent; the log ends with `all modules verified` and `backend checks passed`. Primary independently confirmed the seven correction hashes still match, the protected graph/trace/runtimecache/dependency sources remain unchanged and HEAD is still0387cbf391da61df3afa6ae0a357d6a754905fd7. Task3.5 is now accepted. Evidence remains outside disposable roots; the first failed run is preserved. Synthetic benchmark and fixture/race success do not establish production-scale latency or frontend/live E2E acceptance.

Frontend read-only integration preflight loaded the current Impeccable/Operate guidance, PRODUCT/DESIGN, matching App.vue surface brief, route owner, query model and API decoder. `context.mjs --target frontend/src/features/query` and the targeted surface-brief read ran once. The older surface brief's co-star-only All prose is superseded by the explicit approved PRODUCT/DESIGN/delta; no out-of-scope sidecar repair was made. Fresh pinned `npm run typecheck` returns exit2 solely at `model.ts:842` for the missing `wish` copy mapping; actual evidence is `/root/.hermes/workspace/bangumi-staff-stats/frontend-integration-preflight.json`. This preflight made no frontend implementation edits. Read-only owner/test-seam audit `deleg_bfe8f015` completed without changes or test runs. Primary independently inspected model validation/projections, selector scope events, query recovery, the real candidates driver, coordinator identity checks, App.vue handoff and person-detail/candidates schemas. The audit identified two separate integration risks: query-wide all must not reuse the legacy operation-only scope, and ranking→co-star cannot obtain complete identities from empty query keys or paginated detail contributions. Candidate rows already carry complete backend-authoritative identities, but their real driver and coordinator currently reject unrestricted/hidden-key responses. No response-contract expansion or frontend statistical reconstruction is authorized by this finding. The bounded query-core/recovery implementation is assigned to `deleg_abfeb622`; selectors, cross-mode integration and entry/detail remain separate pending slices. Read-only architecture resolution `deleg_180f29df` completed without edits/tests and confirmed that the existing unfiltered candidates projection can supply complete identities: look up an exact numeric person ID, then consume that one row's full identity array without statistical aggregation, catalog expansion or detail-page union. Primary verified `candidates/build.go` constructs identities before pagination, the search only narrows server-side names, and the response contract has no exact-ID lookup field. The bounded handoff will reuse the transient workspace-links owner, preserve the Applied Query, omit participants, check transaction/query/revision/target/data-and-collection snapshot on every page, and change selection/route only after a complete valid 1–20-identity result. Cancel/new primary start/later selection must invalidate success, rejection and finalization; paging must make validated progress, not silently impose a not-found cap. The worst-case lookup is not a bounded-latency guarantee. The audit also correctly distinguishes ready character-section availability (already driven by authoritative `summary.characterCount` presence) from skeleton hints; no ready-section repair is needed. Primary verified a further admission boundary requiring explicit specification in the cross-mode slice: catalog-absent keys can defer to factual backend validation only for query-wide all; a known hidden staff entry is represented by kind=staff and selectable=false with empty capabilities, so an unconditional capability/visibility rejection would still drop valid participation. `factualOperationPlans` also includes exact staff irrespective of catalog capability metadata and canonical cast-all for anime/game. Selector eligibility and concrete/legacy operation guards must stay separate from permission to request these factual identities; wrong-type known entries and explicit non-factual roles/sets retain their guards. No frontend prefix parsing or returned-key authority cache is planned. This technical admission rule remains to be documented and reviewed before the cross-mode implementation. Backend source and its passed full-gate snapshot remain frozen.

## Frontend query-core recovery checkpoint — core reviews approved

`deleg_abfeb622` timed out at600s without a final summary. Primary recovered the actual diff and persisted commands from `/root/.hermes/workspace/bangumi-staff-stats/frontend-core-results.json`, rather than treating timeout as either success or no output. Production edits are confined to `src/features/query/model.ts` and `recovery.ts`; existing store/session behavior is exercised without changing those owners. New and changed tests cover optional query-wide all, strict malformed/exclusive scope rejection, legacy no-scope behavior, wish normalization/type-specific summary labels, semantic dirty/undo and recovery/session identities. Recorded test-first RED is40failed/157passed across five files, followed by a model-green/recovery-red run with5failed/192passed, then197/197passing and typecheck exit0. Four earlier wrapper/OpenSpec invocation failures are environment errors, not product RED. The timed-out final patch reported an error despite changing comments; primary inspected the resulting source and reran verification on the actual final files.

Primary's independent pinned `npm test -- --maxWorkers=2` from frontend returned exit0:50test files/674tests passed in137.06s. Separate pinned `npm run typecheck` and root strict OpenSpec/diff checks returned exit0. Evidence `/root/.hermes/workspace/bangumi-staff-stats/frontend-core-parent-checkpoint.json` records commands, outputs, current owned hashes and493protected paths with no change. This supersedes the missing-wish typecheck failure, but is not the full `npm run check` component gate or all frontend behavior acceptance. At that test checkpoint the newly added store test had not yet been enrolled; the subsequent exact enrollment is recorded below. Independent core-only SPEC review `deleg_d80081d8` returned SPEC COMPLIANT with no actionable gap, after independently reading the actual diff and rerunning197/197focused tests, typecheck and diff checks with exit0. Its nine checkpoint hashes matched before and after; primary also rechecked all nine source/test hashes against the tested checkpoint and the unchanged HEAD. Core-only QUALITY review `deleg_5f86e122` returned QUALITY APPROVED with no actionable Critical/Important/Minor findings. It independently reran197/197focused tests, typecheck and diff checks with exit0; all nine checkpoint hashes matched before/after and HEAD was unchanged. Primary independently rechecked the same nine hashes before integration. This closes core-only SPEC/QUALITY, not whole task4.2 or feature acceptance. Task4.2 remains open because selectors, real-driver/coordinator admission, complete identity handoff and entry/detail integration are still unimplemented.

Primary reconciled the existing design/frontend-query-shell delta/tasks with the read-only architecture resolution: unchanged candidates contract, exact single-row complete identity, atomic/cancellable temporary ownership, selector versus factual-admission separation and no fake all+nonempty accepted Query. Source/test closed-inventory scope permits only exact reviewed path registration with an extra-file rejection control; no checker implementation or guard has been changed. Strict validation and diff hygiene passed after these refinements. These statements specify upcoming integration and do not claim it is implemented.

Read-only frontend architecture preflight ran the unchanged pinned `node scripts/check-architecture.mjs` and returned exit1 at the persistent inventory guard, with no missing paths and exactly two extras: `tests/app/bangumi-plugin.test.ts` and `tests/features/query/store-unrestricted.test.ts`. Evidence `/root/.hermes/workspace/bangumi-staff-stats/frontend-inventory-preflight.json` preserves the actual failure and unchanged checker hash. This was not the full frontend gate; no guard was altered or inventory entry registered before quality approval. Read-only native-selector preparation confirmed the existing All row/event can be reused: ranking must clear Draft keys and set shared scope, while legacy co-star All must keep its stored concrete keys. Selecting concrete after an inherited query-all state must remove shared scope, including in co-star; type transitions retain one subjectType and must not mix scopes or reset manual status defaults. Both existing ranking empty-key guards (App loadAppliedMode and QueryWorkspace mode watcher) need the same explicit-query-all exception, not a blanket removal.

After both core reviews passed, primary added only `tests/app/bangumi-plugin.test.ts` and `tests/features/query/store-unrestricted.test.ts` to the existing frontend expectedInventory. Removing those two inserted literal lines reproduces the entire HEAD checker byte-for-byte; no previous entry, checker behavior, dependency set or budget changed. The real pinned architecture checker passed209persistent files, then rejected an actual primary-owned extra probe file at the unchanged inventory guard, and passed again after exact probe cleanup. Strict OpenSpec/diff checks also passed. Evidence `/root/.hermes/workspace/bangumi-staff-stats/frontend-inventory-enrollment.json` records all exits, literal ownership and checker SHA256; this is not the full frontend component gate. Native selector/status implementation with the two narrow ranking-empty UI exceptions was assigned to `deleg_84d5b1cd`, with no coordinator/candidate/entry/delivery writes and a persisted RED/GREEN checkpoint requirement.

### Native selector recovery — SPEC and QUALITY approved for native controls

`deleg_84d5b1cd` timed out after 600 seconds without a final summary. Primary inspected its durable checkpoint, completed transcript, actual working-tree diff and owned paths. The final production patch did land despite the cancelled-tool report: five production and three existing test files contain the bounded selector/status and ranking-guard slice. No implementer GREEN or review approval is inferred from that write. The first RED and timeout evidence remain in `/root/.hermes/workspace/bangumi-staff-stats/frontend-selector-results.json`.

Primary reran the complete three-file selector/reveal/App-ranking suite with pinned tools: exit 1, 96 tests total, 88 passed and 8 failed. Independent `npm run typecheck` returned exit 0. `/root/.hermes/workspace/bangumi-staff-stats/frontend-selector-parent-checkpoint.json` preserves exact commands, full outputs, exits, eight owned-file hashes and the comparison of 1,613 protected paths with no mismatches. The implementer's aggregate protected-digest mismatch is attributable to primary's authorized concurrent `verification.md` update, not a changed protected source file. Existing core/architecture ownership checks also matched; backend remains frozen.

Source-level diagnosis separates three causes: six focus regressions when clearing concrete keys replaces the selector row ID before focus restoration; missing explicit disabled ARIA on the status checkboxes (the installed Naive UI disables interaction but does not emit `aria-disabled`); and one incorrect new App test expectation that ignored the existing first-response snapshot revision increment. The coordinator's revision rule is not a product defect and must not be changed to satisfy that assertion.

Bounded correction owner `deleg_5b3cad9b` returned DONE after changing four allowed files: the surviving-row focus fallback with disabled/unmounted guards, local checkbox ARIA, four later-row focus regressions across ranking/co-star and desktop/compact, stronger disabled click/Space/Enter assertions, and the new App test revision expectation only. The actual eleven command records in `/root/.hermes/workspace/bangumi-staff-stats/frontend-selector-fix.json` preserve initial eight-failure RED, four-failure later-row RED, focused GREENs, one-failure disabled RED and its GREEN, 100/100 three-file tests, typecheck, architecture and diff checks. No coordinator, candidate, core, entry or delivery changes were made.

Primary reread the complete eight-file native-control diff and independently reran all frontend unit tests: 50 files / 712 tests passed, exit 0; typecheck and architecture also returned exit 0, with 209 persistent files. The eight source/test hashes stayed unchanged throughout. `/root/.hermes/workspace/bangumi-staff-stats/frontend-selector-parent-green.json` records exact commands, complete outputs and ownership checks. The only additional difference from the correction owner's baseline was primary's subsequent authorized `verification.md` edit; reconstructing its exact inverse reproduced the original baseline SHA256, with no unexpected protected changes.

Native-control-only independent SPEC review `deleg_f5faf017` returned SPEC COMPLIANT with no actionable in-scope gaps after inspecting the complete eight-path diff and surrounding authorities. Its fresh pinned run passed 3 files / 100 tests, with exit 0; root diff check and the unchanged coordinator comparison also returned exit 0, and all eight checkpoint SHA256 hashes matched before and after. It verified Node 24.18.0 / npm 11.16.0 but did not rerun the complete unit suite or typecheck. Primary subsequently reconfirmed the eight hashes, HEAD and unchanged coordinator. Native-control-only QUALITY review `deleg_dc1f9b1b` returned QUALITY APPROVED with no actionable findings. It independently reran the three-file 100/100 test suite, typecheck and root diff check with exit 0, verified pinned Node/npm, and preserved all eight checkpoint hashes and HEAD; coordinator remained identical to HEAD at that checkpoint. Primary reconfirmed those eight hashes and unchanged next-slice source seams before assigning new implementation. Task 4.2 remains incomplete because identity admission, lazy handoff and subsequent entry/detail/delivery integration are not yet accepted. The 712-test result is not the full `npm run check`, production build, browser acceptance, full-feature review or release approval.

Read-only preparation for the next identity slice confirmed the actual remaining seams: `api/candidates.ts` currently derives row projection keys from response positionCounts only for operation-level all; `coordinator.ts` central operation admission currently reuses query membership and catalog selectable/capability checks. The adopted query-all exception must remain bounded and metadata-based, not a global relaxation or key-prefix parser. Existing `PersonItemBrowser.vue` and `PersonInspector.vue` already use characterCount presence (including zero), so no speculative character-tab rewrite is justified. No identity implementation was performed during the native-control review.

### Operation identity admission — SPEC and QUALITY approved

After native-control SPEC and QUALITY approval, `deleg_ea53b723` owns the bounded query-all admission and actual candidate projection slice. Writable paths are only `frontend/src/api/candidates.ts`, `frontend/src/api/adapters/queryWire.ts` (minimal existing-schema scalar decoder export if needed), `frontend/src/features/query/coordinator.ts` (central admission/input validation only), `frontend/tests/api/candidates.test.ts` and `frontend/tests/features/query/coordinator.test.ts`. Core and native-control checkpoints remain read-only. Opaque identities still must satisfy the existing shared `PositionKeyV1` schema; schema validation must not be confused with prefix-based permission inference or proof of factual existence. The implementer returned DONE and persisted eight command records, real RED/GREEN, exact five-file ownership and preservation evidence at `/root/.hermes/workspace/bangumi-staff-stats/frontend-identity-admission-results.json`. Actual source changes add the existing-schema scalar decoder, metadata-based query-all permission, existing input decoders before legacy canonicalization, and the real candidates-driver response-universe predicate. No UI, linked-detail, handoff, entry or delivery implementation is claimed.

The first RED had 13 failures including a Pinia test-harness state leak; after clearing the inherited flag in the legacy fixture, confirmed RED had 12 failures / 142 passes. The initial GREEN attempt had one failing newly added duplicate-person response assertion, which was removed with an explicit scope note; final focused verification passed 4 files / 153 tests, and typecheck, architecture (209 files) and diff checks returned exit 0. Incumbent API tests and the legacy coordinator anchor remained unchanged. The removed assertion is not being counted as an established incumbent guard: the existing API driver/adapter do not separately reject repeated person IDs. Its removal and the preservation wording are explicitly included in independent SPEC review; the future lazy handoff still must check person/page uniqueness as specified.

Primary reread the complete five-file diff and the actual candidates adapter, verified all 1,636 protected paths and 17 core/native hashes, then independently ran all frontend unit tests: 50 files / 736 tests passed, exit 0. Typecheck and architecture also returned exit 0; the five owned hashes matched before/after. `/root/.hermes/workspace/bangumi-staff-stats/frontend-identity-admission-parent.json` preserves exact commands, full outputs, hashes and the disclosed removed-test note. This is not a full frontend component/build/browser gate. Independent identity-admission-only SPEC review `deleg_38923fa2` returned SPEC COMPLIANT with no actionable in-scope gaps after inspecting the complete five-file diff. It freshly passed the four-file 153/153 test suite, typecheck and root diff check with exit 0, while preserving five owned, 17 core/native and 1,636 protected hashes and HEAD. It accepted removal of the newly proposed duplicate-person response assertion after confirming that no incumbent test line or uniqueness guard was removed; this is explicitly not evidence that the driver rejects repeated people. Future handoff person/page uniqueness remains mandatory. Identity-admission-only QUALITY review `deleg_c8235e72` returned QUALITY APPROVED with no Critical, Important or Minor findings. Its fresh four-file 153/153 suite, typecheck and root diff check returned exit 0, with all five owned, 17 core/native and 1,636 protected hashes and HEAD preserved. Primary independently reconfirmed all three hash sets and HEAD after that review before assigning the next component slice. Task 4.2, whole-feature acceptance, full frontend gate, browser acceptance and delivery remain open.

### Linked-detail and complete-row lookup component — SPEC and QUALITY approved

After admission dual review, `deleg_efd986e4` owns exactly `frontend/src/features/person-detail/workspaceLinks.ts`, `frontend/src/features/person-detail/components/PersonInspector.vue`, `frontend/tests/features/person-detail/workspace-links.test.ts` and `frontend/tests/features/person-detail/components.test.ts`. Primary reread the actual owner, inspector, API driver/adapter and tests. The component must preserve the genuine Applied Query for explicit detail, display accepted-input identities, narrow only the legacy empty-key ranking-location block, and implement the specified cancellable exact-ID complete candidate-row lookup. Existing ready characterCount presence semantics, including zero, are preserved rather than speculatively rewritten.

The lookup must independently reject repeated people/pages and validate context, snapshot and forward progress; the upstream driver does not establish person uniqueness. It exposes transient pending/error/cancel and caller-ownership integration, without mutating selection, route, Draft or visible candidate resources. App-side atomic commit and immediate invalidation on primary-request start remain a separate subsequent integration owner, not completion criteria silently dropped from task 4.2. The pre-implementation `/root/.hermes/workspace/bangumi-staff-stats/frontend-workspace-links-parent-checkpoint.json` records four verified owned baselines and 1,637 protected hashes; the implementer owns a separate incremental `frontend-workspace-links-results.json` evidence file outside the repository. The implementer timed out at 600 seconds without a summary. Primary recovered the actual four-file diff and 15 incremental command records rather than treating timeout as success or no work. The record contains failing tests before production changes, corrected schema-real identity fixtures, a 21-identity regression caught during the first GREEN attempt, then 92/92 focused tests and typecheck with exit 0. An earlier wrong-cwd preservation probe failed; the corrected probe and primary hash verification succeeded. The implementer did not record the architecture gate before interruption.

Primary independently passed the two-file 92/92 suite, typecheck and architecture (209 persistent files), then all 50 frontend unit-test files / 788 tests, each with exit 0. The four owned hashes stayed stable; all 1,637 protected hashes matched and HEAD remained unchanged. A truncated raw git file-list comparison was rejected as invalid evidence, then replaced by complete subprocess capture and direct existence checks: no new nonignored files and no missing initial files. The baseline list includes ignored disposable files, so it is not compared blindly with git inventory. Exact commands, outputs, current hashes and corrected inventory evidence are in `/root/.hermes/workspace/bangumi-staff-stats/frontend-workspace-links-parent.json`. These are unit/type/architecture checks, not the full frontend component/build/browser gate.

The implemented caller API is `lookupCandidateIdentities(target, isCurrent): Promise<readonly string[] | null>`, `cancelCandidateLookup()`, and `candidateLookupPending` / `candidateLookupError`. It returns only one exact complete identity list, with explicit 20-identity admission and independent page/person uniqueness checks; it never commits selection or navigation. Independent component-only SPEC review `deleg_1578b8e6` returned SPEC COMPLIANT after reviewing the complete four-file diff. Its fresh 92/92 focused suite, typecheck and diff check returned exit 0, and it reported five additional in-memory controlled probes including preservation of a newer completed error and exact-ID discovery on page 6. It assessed same-controller stale finalization as bounded cleanup that cannot clear a newer controller’s pending state or completed error. All four owned and 1,637 protected hashes, HEAD and file inventory were preserved; primary reconfirmed those hashes and HEAD after review. The reviewer identified nonblocking test hardening: replacing the ordinary harness’s fail-fast candidates stub removed an implicit unexpected-call guard; explicit zero-candidate-call assertions could restore it. Component-only QUALITY review `deleg_79113baf` returned QUALITY APPROVED with no Critical or Important findings after inspecting the complete four-file diff. It independently passed 92/92 focused tests, typecheck and diff check, plus five controlled post-adapter probes; the four owned and 1,637 protected hashes and HEAD remained unchanged. Its sole Minor repeated the lost ordinary-harness unexpected-candidate-call guard. Primary then made a test-only opt-in fixture correction: ordinary harness calls again receive the original throwing candidates driver, while only `lookupHarness` opts into the deferred driver. Fresh primary 92/92 focused tests, typecheck and diff check returned exit 0; three production/other owned hashes and all 1,637 protected hashes remained unchanged. `/root/.hermes/workspace/bangumi-staff-stats/frontend-workspace-links-harness-hardening.json` preserves the reviewed hashes, exact test-only delta and post-fix commands/results. This small fixture correction is parent-verified, not a new independent review or whole-feature approval. App-side current-detail admission, primary-start invalidation and atomic navigation remain unimplemented; task 4.2 stays open.

### App atomic query-all handoff — SPEC approved, QUALITY pending

After component dual review and parent verification of the small test-harness correction, `deleg_591c0b7b` owns only `frontend/src/app/App.vue` and `frontend/tests/app/person-workspace-links.test.ts`. It consumes the approved temporary lookup and must wire ready/current-detail admission, genuine pending/disabled controls, atomic selection + operation-all scope + navigation, and cancellation on actual primary-driver start, target/selection/mode/cancel/unmount. Primary-start interception belongs in the existing App presentation-driver wrappers; the independent raw lookup driver must not invalidate itself, and the existing co-star-to-ranking navigation sequence remains separate. No coordinator/core/component rewrite, new state owner, entry/delivery implementation or new persistent repository file is authorized in this slice. The parent checkpoint `/root/.hermes/workspace/bangumi-staff-stats/frontend-app-handoff-parent-checkpoint.json` records both complete owned baselines, 1,639 protected hashes and 1,642 nonignored files; only this verification document is separately parent-writable. Incremental real RED/GREEN evidence belongs in `/root/.hermes/workspace/bangumi-staff-stats/frontend-app-handoff-results.json`. The first App implementer timed out at 600 seconds / 27 calls. Primary recovered the full two-path baseline diff and three persisted run records: only the App test changed, adding 30 cases to the seven incumbents; App production remained unchanged. Its 30-failure / seven-pass RED included a global-fixture mismatch, not 30 independently established product defects. The global co-star golden is series-based while the test query is unmerged subject-based, so `CoStarSurface.vue:97–114` correctly withheld the mismatched ready payload. Global partners had the same unit mismatch; the newly invented `opaque-*` test keys also violated the existing PositionKeyV1 schema. Primary corrected only fixture construction before real decoding and replaced those keys with schema-valid unknown staff IDs, preserving every assertion and all protected files. The corrected personal/global success tests now both reach the intended missing-linked-request RED (two failed, 35 skipped), with App production still at its pre-slice hash. Exact diagnosis and actual rerun are in `/root/.hermes/workspace/bangumi-staff-stats/frontend-app-handoff-fixture-recovery.json`; the original attempt remains separately preserved. `deleg_3eb00e5a` continues the same two-path implementation from these existing tests and writes incremental evidence to `/root/.hermes/workspace/bangumi-staff-stats/frontend-app-handoff-continuation.json`. The continuation returned DONE for the bounded App slice. Primary inspected the complete actual two-path diff and its eight command records. The matching personal/global filter reached GREEN (two passed / 35 skipped); the implementer passed all 37 App cases plus 92 helper/component cases (129 total), additional rankings integration 17, typecheck, architecture 209 and diff check. New source uses the existing presentation-driver start seam and a separate monotonic handoff sequence, current-ready detail admission, helper pending/error/cancel, synchronous intent invalidations and validated selection-before-cancel atomic commit. The original tests and repaired fixtures remain; exact Applied object identity and ordinary zero-lazy-request assertions were added. Primary independently ran the entire frontend unit suite: 50 files / 818 tests passed, exit 0; typecheck, architecture (209 persistent files) and diff checks also returned exit 0. Both current owned hashes stayed stable, all 1,639 protected hashes matched, no nonignored files were added or lost, and HEAD remained unchanged. Exact parent commands, outputs and hashes are in `/root/.hermes/workspace/bangumi-staff-stats/frontend-app-handoff-parent.json`. Independent two-path SPEC review `deleg_cb17b9d2` returned SPEC COMPLIANT with no actionable requirements gaps. It inspected both actual diffs and TDD provenance, verified current ready detail/primary-start and intent ownership/synchronous validated commit/shared drawer controls, and freshly passed 129 tests across three files, typecheck and diff check with exit 0. Both owned hashes, all 1,639 protected hashes, 1,642 nonignored files, HEAD and master stayed unchanged; no edits or ref writes occurred. Primary independently reconfirmed the current hashes and HEAD. Independent bounded QUALITY review is now assigned to `deleg_a6f3f676`; task 4.2 is not yet closed. These mounted/post-adapter tests do not establish full npm check, build, HTTP/browser/visual acceptance or release readiness. Task 4.2 and whole-feature acceptance remain open pending review and subsequent integration.

Read-only entry/delivery preparation checked the actual route owner, App.initialize, entry delta and production artifact guards. A future parser must inspect the original pathname before root-to-ranking normalization and distinguish invalid entry from ordinary prefill: invalid entry must not fall through into saved-session automatic replay. Catalog retry must preserve valid one-time intent without duplicate execution. The existing packaging walks all validated static files, while the initial-JavaScript budget counts only module entry/preloads; an installable userscript must remain standalone, byte-identical to its root source, and checked for stale/missing copies, not injected into the SPA. Fresh read-only preparation reconfirmed that `personEntry.ts` does not yet exist, `routes.ts` still performs only ordinary path normalization/user prefill, and App.initialize reads saved recovery before catalog loading. Entry integration must intentionally prioritize captured valid/invalid entry disposition over that recovery path without altering ordinary prefill or enabling fragment sharing. No entry or delivery implementation/build has run in this preparation.

Backend/frontend integration, complete frontend and final affected-component gates, installable bundled URL and full-feature reviews remain pending. Backend's recorded full r2 gate remains passed and frozen. No push, PR, merge, release or new deployment is claimed.
