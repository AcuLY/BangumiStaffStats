## Task Boundary

| Boundary | Declaration |
|---|---|
| Status | investigated: complete; specified: approved; implemented: unaccepted updater candidate only; verified: official CLI/Skill/tag/commit, exact 127-file skill, official hook provenance, and strict planning checks passed; full candidate acceptance pending; committed: planning status is determined by containing Git history and final upgrade commit is not yet claimed; pushed: no; released: no; deployed: no |
| Owner | Main owns planning, acceptance, corrections, sync/archive, staging, and commits. One implementation subagent owns tasks 2.1–2.6. |
| Writable paths | Exact product/tooling and lifecycle paths declared by proposal/spec. |
| Protected state | All undeclared repository, host, external, remote, and production state. |
| Stop conditions | Stop on source/version/path/foreign-state conflict or failed acceptance; preserve the bounded candidate without broad cleanup or rollback. |

## 1. Planning — main

- [x] 1.1 Confirm current Skill/CLI/tag/commit facts, Node 24 availability, official updater behavior, and the accidental unaccepted candidate.
- [x] 1.2 Replace the over-specified transaction with this concise updater, adaptation, and acceptance contract; run strict validation and main semantic review.
- [x] 1.3 Mark all four Status rows approved and commit only this change as `docs(openspec): approve Impeccable v4 upgrade`.

## 2. Implementation — subagent

- [ ] 2.1 Read the complete approved change; verify branch/planning HEAD, empty index, current candidate envelope, foreign untracked state, and protected paths before mutation.
- [ ] 2.2 Without rerunning the updater, verify the existing candidate's Skill v4.0.2 header, complete 127-file tree, and Codex hook against official tag/commit.
- [ ] 2.3 Apply only the approved PRODUCT schema migration, exact route surface brief, and bounded root Impeccable ignore block.
- [ ] 2.4 Verify every installed JavaScript file with Node 24 syntax checks; validate skill metadata, Codex hook merge, context, all surface resolutions, doctor JSON, and safe hook smoke behavior.
- [ ] 2.5 Run targeted/all strict OpenSpec, OpenSpec doctor, Git diff checks, and exact changed-path inventory; prove DESIGN/sidecar/editor/Contracts/Wave 1B/runtime/remote state is untouched.
- [ ] 2.6 Remove only apply-created transient state, update tasks 2.1–2.6 truthfully, and hand the unstaged candidate plus concise evidence to main without staging, committing, pushing, or starting Wave 1B.

## 3. Acceptance and lifecycle — main

- [ ] 3.1 Audit source provenance and the complete candidate diff; rerun the material acceptance gates and directly make only simple in-envelope corrections if required.
- [ ] 3.2 Update the four Status rows, sync/archive the capability, stage only the accepted product/tooling/lifecycle envelope, and commit `chore(impeccable): upgrade project skill to v4.0.2`.
- [ ] 3.3 Verify the final commit, empty index, retained foreign state, strict OpenSpec, and no push/release/deploy; hand the accepted v4 baseline to Wave 1B.
