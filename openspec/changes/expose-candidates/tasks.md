## 1. Contract and generation

- [ ] 1.1 Define closed candidate request/success schemas and deterministic global, personal, stale, empty, rank-gap, and error goldens.
- [ ] 1.2 Add isolated Go/TypeScript candidate projection generation and drift checks without changing prior generated bytes.

## 2. Backend production core

- [ ] 2.1 Implement independent per-position candidate set/statistics construction and ordered complete position counts.
- [ ] 2.2 Implement scope-specific view defaults/validation, strict total sorting, rank-before-search, checked pagination, and immutable result-cache integration.

## 3. HTTP and wiring

- [ ] 3.1 Implement strict POST transport, stable result error mapping, metadata/headers, request cancellation, and handler tests.
- [ ] 3.2 After the rankings handoff, register the route and generation/check inventories in the production runtime without fixtures or frontend state.

## 4. Acceptance

- [ ] 4.1 Pass shared goldens, focused/repeated/race, vet, build, full backend and generation gates, and prior-projection byte checks.
- [ ] 4.2 Pass strict OpenSpec, diff, generated-residue, architecture, index, and ref gates; leave an unstaged candidate.
