## Task Boundary

| Field | Boundary |
|---|---|
| Status | Local specified; implementation pending |
| Owner | Frontend shared info trigger; primary agent accepts |
| Writable paths | Exact proposal paths and two auto-open supersession notes |
| Read-only protected inputs | Product/design authorities, help content/state, empty illustration, unrelated icons/features, APIs/contracts/Backend/updater/Archive/external state |
| Deletion complement | Only duplicate info SVG branches and conflicting local presentation; preserve controls/content/events/targets/tests |
| Mutable refs | Current dirty worktree and task markers only |
| Consumes | Query reference, three icon wrapper APIs, native buttons, Naive Tooltip/Popover |
| Produces | Shared InfoIcon and `.info-trigger` contract across five trigger classes |
| Dependencies | Existing Vue/CSS/tokens; no package |
| Deliverables | Source/style/tests plus focused build/browser/OpenSpec evidence |
| Acceptance | Identical 16/24/44 glyph/geometry/radius/states; behavior/a11y/empty illustration preserved |
| Non-goals | Copy/placement/data/empty illustration/unrelated icons/broad cleanup |
| Operations deferred | Full gate, sync/archive, Git/release/deploy/host mutation |
| Stop/rollback conditions | Stop on ownership drift, Tooltip/Popover/a11y/target/layout/theme/illustration/test regression or undeclared write |

Forbidden: reset/checkout rollback, git clean, `git add -A`, broad deletion,
undeclared/generated/Backend/external writes, and production operations.

## 1. Planning and ownership — primary

- [x] 1.1 Verify branch/HEAD/dirty paths, audit all info uses/styles/tests,
  add exact supersession notes, require strict-valid reviewed artifacts and zero
  unresolved P0/P1 before apply.

## 2. Shared glyph and trigger implementation — primary

- [x] 2.1 Add shared InfoIcon matching the query reference and delegate only the
  `info` branch from QueryIcon, AppIcon, and CoStarIcon while preserving their
  public APIs, classes, and every non-info glyph.
- [x] 2.2 Add `.info-trigger` to UID/query option/position, stat evidence,
  partners metric, and series identity buttons; move 16/24/44 geometry, 6px
  radius, tokens, hover and focus to one base rule.
- [x] 2.3 Delete only conflicting person/partners/series presentation rules,
  preserve all Tooltip/Popover owners, and keep the 28px co-star empty-state
  illustration non-interactive.

## 3. Focused regressions — primary

- [x] 3.1 Add a shared source/component regression for one InfoIcon owner,
  wrapper delegation, trigger-class coverage, reference geometry/states, and
  decorative-icon exclusion.
- [x] 3.2 Update focused query/person/partners/co-star assertions so each trigger
  retains accessible name, expanded/focus/click/Escape behavior and shared class.

## 4. Focused acceptance — primary

- [x] 4.1 Run shared/query/person/partners/co-star Vitest, Vue typecheck, Vite
  build, Impeccable detector, strict validation, and `git diff --check` without
  the complete accumulated gate.
- [x] 4.2 Browser-check query, person evidence, partners metric, and series help
  where data permits in Light/Dark and 599/605/961px: computed glyph/16/24/44/
  radius/colors, hover/focus/open/Escape, target spacing, scrollWidth, overlay,
  and fresh console logs.
- [x] 4.3 Keep root spec sync/archive, complete accumulated gate, Git integration,
  release/deployment, and all host/production mutation deferred.

  Evidence: six focused Vitest files passed 86/86; Vue typecheck and Vite
  production build passed; Impeccable detection returned `[]`; this change and
  its superseded person owner are strict-valid; `git diff --check` passes.
  Browser measurements across query, person evidence, and partners metric in
  Light/Dark at 599/605/961px produced nine samples and one exact geometry
  signature: shared glyph paths, 1.9 stroke, 16px icon, 24px visible box, 6px
  radius, transparent surface, and -10px pseudo hit inset. Default/hover-focus,
  `aria-expanded`, open and Escape behavior matched the query reference;
  document scrollWidth equaled clientWidth and no framework overlay or fresh
  console warning/error appeared. Series help had no matching live result in
  the accepted data and is covered by the shared source/feature regression;
  the 28px decorative empty-state icon remains outside `.info-trigger`. Full
  gate and every lifecycle/external state remain deferred.

  Evidence so far: six focused Vitest files passed 86/86; Vue typecheck and
  Vite production build passed; Impeccable detection returned `[]`; this change
  and its superseded person owner are strict-valid; `git diff --check` passes.
  Browser acceptance remains pending because local ports 5174 and 8080 are not
  listening. Full gate and every lifecycle/external state remain deferred.
