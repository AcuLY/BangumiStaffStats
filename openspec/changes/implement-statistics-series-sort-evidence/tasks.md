## Task Boundary

| Boundary | Declaration |
|---|---|
| Status | investigated: complete; specified: approved; implemented: no; verified: main semantic/dependency review and strict validation passed; committed/pushed/released/deployed: no |
| Owner | One Contracts subagent owns group 1; after accepted handoff, one Backend subagent owns group 2; the main agent owns specification decisions and group 3 acceptance. |
| Writable paths | Contracts: `contracts/goldens/statistics/**` and this file's group 1 markers. Backend production files: `backend/internal/statistics/{doc,errors,types,decimal,rating,series,source,evaluator,preference,summary,sort}.go`. Backend tests: `backend/internal/statistics/{decimal,rating,series,source,evaluator,preference,summary,sort,golden,archive_integration,property,benchmark}_test.go`. Backend integration inventory: `backend/internal/architecture/dependencies_test.go`, `backend/scripts/check.sh`, `backend/README.md`, and this file's group 2 markers. Main acceptance may modify only group 3 markers. Planning changes before approval remain limited to `openspec/changes/implement-statistics-series-sort-evidence/**`. |
| Read-only protected inputs | `PRODUCT.md`, `DESIGN.md`, `tmp-formal-development/**`, oracle `644b7748674e553f863d0ffd61d029f86fdc0717`, all root specs and other changes, every existing contract/golden outside the new owned root, `backend/internal/query/**`, `backend/internal/archive/**`, `backend/internal/httpapi/**`, every other backend/frontend/updater path, `.vscode/**`, Git refs/remotes, other repositories, hosts, and production. |
| Deletion complement | None; no owner may delete, rename, replace, or generate files outside its exact set. |
| Mutable refs | None during apply; owners do not stage, commit, sync/archive, update a branch/ref, push, open a pull request, tag, release, deploy, or activate. |
| Consumes | Accepted exited `implement-query-result-set`; protected rating/date/relation and exact contribution facts; approved product/data authorities; immutable oracle evidence. |
| Produces | Closed statistics corpus/verifier; Go statistical units, series index, metrics, preference, distributions/timelines, summaries/evidence, and strict sorted indexes. |
| Dependencies | Exact direct apply dependency: `implement-query-result-set`. Groups 1 and 2 SHALL NOT apply until it is accepted and exited; group 2 additionally waits for accepted group 1 handoff. |
| Deliverables | `contracts-statistics-goldens`, `backend-statistics-authority`, exact production/tests inventory, and recorded gate evidence. |
| Acceptance | Two-pass cross-language corpus verification; preserve/delta audit; all score/series/preference/summary/sort sentinels; cancellation/immutability/determinism/fuzz/property/race evidence; full Backend and strict OpenSpec gates; clean owned diff. |
| Non-goals | HTTP/OpenAPI/DTOs, search/page, cache, collection client, frontend, query filtering/set algebra changes, Archive/schema/catalog production, or operations. |
| Operations deferred | Activation/rollback, production roots, scheduling, services/proxies, monitoring rollout, secrets, migration, release, deployment, cutover, and legacy deletion. |
| Stop/rollback conditions | Stop before mutation on branch/HEAD/dependency/review/path/index/dirty-state mismatch. Stop on authority/golden/protected-path drift, decimal or comparator ambiguity, write-capable access, race/nondeterminism, or failed gate. Rollback may remove only the owner's unstaged new files or reverse its exact unstaged hunks. `reset --hard`, checkout-based rollback, `git clean`, `git add -A`, broad recursive deletion, outside-path writes, and external mutation are forbidden. |

## 1. Contracts Statistics Goldens

- [ ] 1.1 Preflight `codex/formal-rewrite`: record exact `HEAD`, prove `implement-query-result-set` is accepted and exited, prove all four artifacts here are strict-valid and main-reviewed, inventory the index and allowed dirty paths, hash protected authorities/dependency handoff, and stop without mutation unless `contracts/goldens/statistics/**` is clean and disjoint.
- [ ] 1.2 Add `contracts/goldens/statistics/index.json`, bounded JSON cases, authority/provenance metadata, and zero-dependency `verify.mjs`; enforce unique IDs, exact paths/SHA-256, referential closure, canonical order, reduced rationals, and `PRESERVE_ORACLE`/`INTENTIONAL_DELTA`/`NEW_CAPABILITY` completeness without bulk or personal fixtures.
- [ ] 1.3 Add rating cases for valid `[1,10]`, `0`/null/missing, invalid sentinels, empty/all-unrated/mixed inputs, exact `8.20`, `[6,7,7] -> 6.66`, average floor, overall positive-half-up, five neutral score-5 units, explicit 1–10 `ratingCount`, ten distribution buckets including `.5` boundaries, and subject timeline precision.
- [ ] 1.4 Add series cases for every merged/non-merged relation, same-type/transitive membership, invalid/missing/cross-type fallback, minimum-ID Series ID, exact sequel-order/representative, complete versus matched members, raw intersection before merge, equal series weighting, contribution counts, distribution, and empty series timeline.
- [ ] 1.5 Add personal/global preference and complete-summary/evidence cases covering one-side missing, exact neutral zero, sparse/dense equal scores, subject/series evidence fields, global no-personal output, overlapping person/unit/character de-duplication, and stable attribution ordering.
- [ ] 1.6 Add every approved person/work/series/combination sort chain in both directions, including valid/missing mixtures, fixed secondary directions, complete ties, shuffled inputs, stable scalar/tuple IDs, entity-set preservation, and page-boundary-sized cases.
- [ ] 1.7 Run `node contracts/goldens/statistics/verify.mjs` twice and prove identical inventory/result hashes; run `git diff --check -- contracts/goldens/statistics openspec/changes/implement-statistics-series-sort-evidence/tasks.md`, `openspec validate implement-statistics-series-sort-evidence --strict`, and `openspec validate --all --strict`; hand off only the unstaged owned diff with exact investigated/implemented/verified/committed/pushed/released/deployed states and commands/results.

## 2. Backend Statistics Authority

- [ ] 2.1 Preflight `codex/formal-rewrite`: record exact `HEAD`, prove the direct dependency and Contracts handoff are accepted/unchanged, verify strict-valid/main-reviewed artifacts, inventory index/dirty state and every exact Backend path, hash protected query/archive/golden inputs, and stop without mutation on any mismatch.
- [ ] 2.2 Implement exact validated decimal/rational primitives and stable domain errors in the enumerated files; cover `[1,10]`, unrated versus invalid, negative-zero normalization, floor-to-hundredths, positive-half-up overall, exact comparison, overflow resistance, and no dependency or `go.mod`/`go.sum` change.
- [ ] 2.3 Implement the immutable `dataVersion`-bound series source/index and versioned sequel ordering through fixed argument-bound read-only Archive access; cover relation boundaries, same-type union-find, fallback singletons, minimum-ID Series ID, representative/member order, cancellation, canonical publication, and shuffled relation determinism.
- [ ] 2.4 Implement canonical subject/series unit materialization plus average, rated-unit count, overall, 1–10 vote-count validation, distribution, and timeline; preserve complete versus matched members and exact contributions, weight valid series equally, and emit no series timeline or partial result.
- [ ] 2.5 Implement personal preference, global no-personal-access, complete aggregate summaries, and immutable ordered evidence with all six preference fields, globally unique subject/series/character counts, valid/comparable unit IDs, and exact identity/contribution attribution.
- [ ] 2.6 Implement named count/average/overall/preference/work-or-series/combination sort profiles as new immutable ID indexes; direction changes only the primary value, missing remains last, fixed secondary directions remain fixed, and stable scalar/tuple identity is the final comparison.
- [ ] 2.7 Consume every Contracts case directly in Go; add focused unit and Archive/query integration tests, comparator-law property tests, bounded decimal/sort fuzz targets, cancellation at every long phase, no-partial-publication/input-reuse checks, shuffled/repeated/concurrent determinism, immutability, and representative memory/time benchmarks. Update only the enumerated architecture/check/README paths.
- [ ] 2.8 From `backend/`, run `go test ./internal/statistics/...`, `go test ./internal/statistics/... -count=20`, the bounded decimal and comparator fuzz targets, representative benchmarks with allocation reporting, `go test ./...`, `go test -race ./...`, `go vet ./...`, `go build ./...`, `go mod verify`, and `GO_BOOTSTRAP=/opt/homebrew/bin/go ./scripts/check.sh`; from repository root run the corpus verifier twice, `git diff --check` on exact owned paths, `openspec validate implement-statistics-series-sort-evidence --strict`, `openspec validate --all --strict`, and `openspec doctor`. Hand off only the unstaged exact owned diff with commands/results and all delivery states recorded separately.

## 3. Main-Agent Acceptance

- [ ] 3.1 Audit the complete Contracts and Backend diff against the authority order, exact writable inventory, direct dependency/handoff hashes, approved oracle preservation and intentional deltas, corpus expectations, no-new-dependency claim, package direction, decimal/series/preference/summary/sort semantics, immutable evidence, and all non-goals; reject any golden changed merely to fit implementation.
- [ ] 3.2 Re-run two corpus passes; all targeted/repeated/fuzz/property/benchmark Backend checks; repository-wide test/race/vet/build/module/check gates; exact-path `git diff --check`; strict change/all validation; and `openspec doctor`. Record specified/implemented/verified/committed/pushed/released/deployed separately, require zero P0/P1 findings, and only then authorize later lifecycle work.
