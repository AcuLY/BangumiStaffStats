# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是希望分析自己 Bangumi 收藏数据的用户，尤其是关注动画、书籍、音乐、游戏 Staff、声优和创作者的人。他们会在输入 Bangumi ID 后，通过条目类型、职位、收藏类型和其他条件筛选数据，查看 Staff 排名、参与作品、平均分与加权综合分。

次要用户是希望基于 Bangumi 全站数据研究 Staff 参与作品、评分和数量关系的数据爱好者。

## Product Purpose

把分散在 Bangumi 条目和收藏记录中的 Staff 关系整理为可筛选、可比较的排行榜，帮助用户快速理解自己的内容偏好及相关创作者。

成功意味着用户无需理解数据处理过程即可完成查询；筛选条件和统计口径容易理解；排名结果可信、便于比较和继续浏览；桌面与移动端都能清楚查看密集数据；加载、无结果、错误和取消状态表达明确。

## Positioning

基于 Bangumi 收藏与全站数据，把条目关系转换为可筛选、可比较的 Staff 排名。

## Query Application Contract

### Query model

人物排行与共演分析共用一份 Query Draft、Applied Query 和有序职位集合。修改 draft 时保留已应用查询与两个模式各自的现有结果；只有“应用查询”才产生新的 query revision。模式切换不应用未提交的 draft，也不重置查询或人物结果；目标模式缺少当前 revision 的结果且已应用条件适用于该模式时，直接用同一 Applied Query 加载目标结果。例外是仅含“全部”范围、没有具体职位的查询进入人物排行：此时展开职位编辑器，由用户指定排行职位，不发送无效请求。

- 人物排行允许选择一个或多个职位。人物必须同时具备全部所选职位；该人物在这些职位下的参与作品取并集，并按 Subject ID 去重后计算指标。
- 共演分析允许选择一个或多个职位作为初始来源候选范围，每个职位独立产生候选人数和单职位列表。选定来源人物后，候选浏览扩展到当前条目类型的全部可用职位，并只保留与当前已选组合存在同一原始共同作品的候选身份：每个人先合并自己所选身份的作品，再对所有已选人物求交集，候选身份必须命中该交集；分别与不同已选人物合作、但没有全体共同作品的人物不属于有效候选。开启合并续作也不改变这个原始作品判断。“全部职位”按人物合并有效候选身份及其完整查询范围内的参与作品，由后端统一排名，每个人只出现一次。切换全部/单一候选职位、搜索、排序和分页由候选接口返回对应页；明确参与身份通过 input.participants 进入语义查询和缓存键，已选样式、部分选中等界面标记仍由前端叠加。已有有效身份可保留为移除或补选入口，不丢失身份管理能力。
- 职位 selector 由动态 catalog 驱动：直接使用 Bangumi 分类，多分类职位在全部上层中出现；“常用职位”和动画/游戏的“配音类”只是同一 canonical 职位集合的快捷展示。任一副本的选中状态同步，已选结果和搜索结果按职位 key 去重；声优 main/all 同类型互斥。
- 常用职位是手工固定顺序，不按使用量动态计算。动画依次为：导演、动画制作、声优（仅主役）、声优、脚本、系列构成、总导演、原作、演出、分镜；游戏依次为：剧本、开发、声优、声优（仅主役）、原画。书籍、音乐和影视在未配置前不显示空的常用区。
- 共演查询的职位控件提供独占的“全部”范围选项，对应操作 positionScope=all；不把全部职位枚举为排行的 AND 条件。从人物详情“查看共演”时选中“全部”和该人物的原有身份，其余已应用条件与未提交 Draft 值保持不变。首次“全部”查询可不指定具体职位；只有 all 范围的候选、合作、共演及显式身份详情允许空 query.positionKeys，排行与普通 query 范围仍要求具体职位。范围参与应用、撤销和标签页恢复；从无具体职位的全部查询切到排行时，先由用户选择排行职位。重新应用新条件仍遵循现有重新选人规则。
- 共演身份以 personId + canonical positionKey 为单位。已选人物 tray 是增加、移除身份和移除整个人物的完整管理入口；分析区只读呈现，身份变化后立即进入对应的单人物合作或多人共演加载态，并只接受最新一次选择对应的响应。
- 首次没有 Applied Query 时自动展开查询编辑器；成功后自动收起。校验失败、请求失败或取消时保持展开并保留 draft。对未变化的查询再次提交必须静默不执行、不折叠编辑器也不显示重复状态。共演查询成功且当前没有选择时，从所选范围（具体查询职位或“全部”）的后端候选顺序原子选择第一位可完整容纳的人物身份，再将候选浏览范围扩展到当前条目类型的全部可用职位；累计超过 20 身份的人物必须整体跳过，不能截断身份或显示用户操作错误。移动端不得因此自动打开人物选择器。精确标签页恢复、已保留选择和候选视图操作不得被该默认选择覆盖，原型 fixture 预选不得进入生产状态。

正式请求使用 `scope=personal|global` 判别联合。两种 scope 共同提交字符串条目类型、两个模式共享的稳定职位 key、NSFW/系列选项和通用过滤条件；personal 额外显式提交 UID、收藏状态、收藏更新时间、个人评分和评分差。global 出现任何个人字段都视为请求错误，不能只隐藏或静默忽略。未启用的范围直接省略，标签提交明确的“正向组间 AND/组内 OR、反向组间 OR/组内 AND”结构，不把 `/`、`+` 输入语法或原型 `enabled` 状态带入 wire。

### Legacy navigation and local recovery

Header 右侧在深浅色主题按钮左边提供始终可用的“回到旧版”跳转图标按钮，同页固定跳转到 `https://search.bgmss.fun/old/`，不附带当前查询参数。旧版入口与主题按钮位于同一个始终靠右的操作容器；桌面显示“回到旧版”，低于 780px 时显示“旧版”，可访问名称仍为“回到旧版”。该入口不以当前模式、查询结果或 catalog 状态为启用条件。

应用不提供查询分享入口、查询链接生成或 URL fragment 回放。初始路由规范化时清除片段且不据此预填或执行查询；普通 `?user=` UID 预填保留。当前标签页恢复只使用前端本地会话状态，不构成公开 URL 或后端契约。

正式部署中新版承载根路径，使用 `/ranking`、`/co-star` 和 `/api/v1/`；旧版通过 `/old/` 访问。原 `/v2/` 页面链接保留查询参数并跳转到对应根路径，旧 `/v2/api/v1/` 和静态资源在切换期间继续兼容；旧版辅助接口保持原有归属。

### Operation result boundaries

- 共演人物的明确参与身份与合作对象职位范围独立。候选、合作、多人共演及显式身份详情可使用 `input.positionScope=all`，在不改变共享 Applied Query、收藏/条目过滤条件和排行职位条件的前提下使用当前条目类型支持的职位；缺省或 `query` 保留原查询职位范围。普通查询先选出原查询职位的第一位人物，之后合作与选人可跨职位；只有一人时，已选人物区提示“点选下方候选人物，查看与已选人物的共演情况”，窄屏收起入口提示“可继续选择人物，进行多人共演分析”；两人以上隐藏。
- 全部职位浏览默认仅采用声优 all 身份，不重复加入 main；用户明确选择的主役身份仍严格保留，并可与其他人物的 all 身份进行共演。跨职位详情、两人/多人分析和当前标签页恢复必须保留每人的实际选中身份。排行定位仍按原榜单条件，不自动扩大职位；未入榜不影响共演或身份详情。

- 人物排行详情提供“查看共演”，沿用 Applied Query，以该人物的完整查询身份替换当前共演选择并进入单人物合作。跨模式切换使用 Header 的普通导航，不记录跳转来源、额外保存旧组合或提供“返回人物排行／返回原分析”入口。联动不应用未提交 Draft，也不受首次默认选人覆盖。
- 共演来源人物和已选参与人物提供独立的“查看详情”入口；合作列表只保留点击人物进入共演的操作，并展示主名、原文名和合作职位。详情按该人物当前选中的职位身份展示，注明身份范围，不改变共演选择；修改选择或应用新查询后退出详情。返回恢复原分析的筛选、分页和浏览位置。
- 共演详情的排行定位独立使用当前人物排行的全部职位条件、排序指标和方向，界面不展示重复的名次与排序摘要。后端按 Person ID 返回完整排序名次和所在页；不入榜显示“未进入当前榜单”，不得以候选或合作名次替代。跳到排行时清除当前列表搜索以定位人物，但保留排序和每页数量；返回分析保留共演状态。
- 人物详情可显式提交非空唯一职位身份；缺省要求在当前 Query 内，all 范围允许当前条目类型的可用职位；只对该人物这些身份的作品计算详情，不要求具备 Query 的其他职位，不扩大公共 Query。未提供子集时保留原排行详情语义；配音角色 section 按实际详情身份范围决定。

- 候选接口同时返回当前操作职位范围内、通过已选组合约束的完整候选人数和当前职位的分页人物；缺省职位范围为已应用职位，all 为当前条目类型可用职位。input.participants 可省略或为空，表示没有已选组合约束；非空时最多 10 位不同人物、20 个唯一身份。无共同作品时返回零候选，不回退全量。候选的作品数和均分仍基于有效身份在完整查询范围中的参与作品；只收窄有效候选集合，不改为共同作品指标。列表搜索后的 total 与完整职位人数分开；名次先在有效候选完整排序结果中确定。增删人物或身份后候选回到第一页，保留候选职位、搜索、排序和每页数量；旧选择对应的候选在加载、失败、取消时不能被误选，重试使用当前选择。移除最后一人后恢复无约束候选但不自动重新选人；会话恢复的首次候选请求直接使用已保存组合。已选、其他身份提示和托盘状态始终由前端本地叠加。
- 人物详情返回人物资料、完整统计摘要、标签、评分图表、偏好证据，以及当前一页作品/系列或配音角色。只有实际详情身份范围包含 cast 职位时才提供角色 section；角色 appearance 始终指向 exact 原始作品，不随系列合并。
- 单人物合作接口返回来源人物、完整合作人数、各指标最高人物、当前排序指标的完整集合刻度 `metricScale` 和分页合作人物，不返回共同作品。点击合作人物后，以来源和目标的精确 identity 调用两人共演接口。
- 共演接口只接受 2–10 个唯一人物且最多 20 个 identity。两人使用 pair 结果；三人以上增加两两关系矩阵。每个人先合并自己的所选身份作品，再对人物做原始作品交集，最后才按系列合并；只参与同系列不同作品不算共同参与。
- operation 的 search、sort 和 page 只改变当前分页列表，不重算完整摘要、标签、图表、偏好或 leaders。合作进度条的 `metricScale` 基于完整合作集合：搜索、翻页、每页数量和升降序不改变同一指标的刻度，切换排序指标时返回该指标的刻度；偏好使用精确分数的绝对最大值，无有效值与有效零值保持区分。单人物合作的职位过滤例外：它会改变合作全集，因此摘要、leaders、刻度和列表一起更新。当前条目类型具有多个可用合作职位时，在合作人物搜索之后提供“全部职位 / 单一职位”筛选；候选职位范围与合作职位筛选各自独立，切换合作职位回到第一页并保留来源人物的已选身份。

### Data sources and metrics

个人收藏和全站数据使用相同的信息结构，但严格分离数据来源：

- v1 个人模式只支持无需用户凭据即可读取的公开收藏，不建立 Bangumi 登录/OAuth，也不接收或保存用户 token、Cookie。公开空收藏是成功空结果；上游明确返回 UID 不存在、私密或禁止访问时显示对应错误，不能伪装成零收藏。
- 当前上下文只有一种评分来源时使用“评分 / 均分”；同处比较两种来源时使用“我的评分 / 全站评分”或“我的均分 / 全站均分”。
- 全站模式不显示个人专属的查询条件、排序选项、评分来源切换、个人评分数量、相对偏好或说明性空区块；只有一个有效来源时整个来源切换器消失。
- 个人人物排行提供作品数、均分、综合分和相对偏好；全站人物排行提供作品数、均分和综合分。
- 综合分统一使用 (n × 均分 + 5 × 5) / (n + 5)。n 是当前评分来源下评分大于 0 的有效作品数；合并续作时是有效系列数。评分人数只用于过滤作品，不参与综合分加权。
- 均分只统计评分大于 0 的作品并向下截取两位；无有效评分时显示“—”。
- 排行摘要始终描述搜索和分页前的完整合格结果：人物数是完整合格人物数，作品/系列数是这些人物实际命中的全局唯一统计单元数；包含声优职位时另显示按 Character ID 去重的角色数。
- 全站排行响应不携带相对偏好；个人排行没有有效评分或足够偏好证据时以缺失值表达，界面显示“—”，不能用 0 冒充。

### Search, sorting and pagination

- 人物、作品和角色搜索只匹配对象的中文名 nameCN 和原文名 name，不匹配别名、展示名、ID、标签、职位或关联对象名称。
- 可见搜索 placeholder 固定为“搜索人物 / 搜索作品 / 搜索角色”；系列浏览器例外使用“搜索系列或系列内作品”。
- 排行、候选人物、作品和角色的排序 selector 只保留已定义的业务指标；缺失主指标始终置后，最终使用稳定实体 ID 保证严格全序。
- 排行名次先按当前排序在完整结果中确定，再执行人物搜索和分页。搜索结果允许出现跳号；只有改变排序指标或方向才重排名次，搜索和翻页不重算摘要或指标尺度。
- 收藏日期排序不得改变结果集合：缺少日期的作品保留并始终置后，不回退到作品日期。系列使用当前查询实际参与成员中最大的非空收藏更新时间，全部缺失时同样置后，不能继承代表条目的隐藏日期。
- 人物、作品和角色分页的 size picker 只提供 5 / 10 / 20；快速跳转输入不设置 placeholder。

### Loading and request continuity

- 数据 Skeleton 只表示真实后端等待且仅替代尚未解析的动态信息；组件、固定文本和已知查询标签保留真实形态。对应界面分包尚未就绪时使用相同业务布局的加载外壳，不显示通用居中四行骨架。打开面板、切换主题、切换密度和其他纯本地交互不得出现伪 loading。
- 当前标签页刷新后，应用恢复当前模式最后一次成功的 Applied Query 与已接受的 operation input/view，并通过普通查询重新获取最新结果。恢复状态不包含响应正文、未应用 Draft、刷新收藏意图、主题或其他瞬态界面状态，存储不可用、无效或重放失败时安全回到可编辑查询，不把旧响应伪装成当前成功。
- 应用新的查询条件时，只替换受影响的结果区；请求失败或取消后恢复上一份已应用结果，draft 继续保留在 Query Editor 中。
- 搜索、排序和分页请求保留已解析的摘要、人物卡、工具栏和输入焦点，只把对应列表行切换为与首次等待同构的 Skeleton；已知分页保留真实组件并处于等待状态，首次无分页数据时隐藏分页，不使用分页 Skeleton。输入 debounce 期间继续显示当前结果，不提前进入 loading。
- 排行先返回人物页，再独立加载当前人物详情；候选列表、已选人物、单人物合作和多人共演也必须互不阻塞。一个分区等待时，已经确定且与该请求无关的内容保持可见。
- 首次 catalog 等待只让职位 selector 显示带加载指示的真实控件，页面 Header、模式和 Query Editor 框架保持可用；加载失败在 selector 内提供重试，不伪装成空目录。
- 快速切换人物、职位、分页或搜索时，旧请求不得覆盖新状态；局部失败保留最后一次成功内容并在相同分区提供错误和重试。

### Shared copy rules

- 一个概念只使用一个稳定名称。面向用户的模式名称是“人物排行 / 共演分析”，加权数量收缩结果称“综合分”，共同参与的作品称“共同作品”；内部技术标识不影响可见文案。
- 来源限定词只在需要比较时出现。单一来源使用“评分 / 均分”，并列来源使用“我的 / 全站”；没有对应概念时不机械添加“我的”。
- Placeholder 只说明当前字段的对象或输入边界，不承担帮助文案。搜索使用“搜索 + 对象”，范围输入成对使用“最低 / 最高”“最少 / 最多”，不加末尾省略号。
- 状态文案陈述当前状态，不用“请先 / 请选择 / 请输入”代替字段校验；可执行动作由按钮或控件承担。
- 组件库默认文案已经准确时保持默认；没有输入提示价值的控件不为了填满界面而增加 placeholder。

## Brand Personality

清晰、可信、有社区感。文案保持直接、克制、数据优先，同时保留 Bangumi 社区工具的亲切感。

主要文化参考是 Bangumi 本身：保留社区归属感、粉色识别和高信息密度，但不照搬其旧式页面结构。

## Anti-references

避免通用 AI 或 SaaS 仪表盘风格、营销型大标题和无意义指标卡、紫色渐变、玻璃拟态、装饰性发光、卡片层层嵌套，以及为留白而牺牲数据浏览效率。

避免只依赖颜色表达状态、在移动端通过缩小到难以阅读的字号塞入全部内容，以及加入与查询任务无关的动画。

## Design Principles

1. **查询优先。** 用户进入页面后，应立即理解如何开始查询。
2. **密集但可读。** 保留数据工具需要的信息量，通过清晰层级而非删减来管理复杂度。
3. **结果值得信任。** 统计口径、加载过程和异常状态都应透明。
4. **熟悉而不陈旧。** 延续 Bangumi 气质，同时使用一致、现代的交互规范。
5. **移动端保留含义。** 通过结构调整适配小屏，而不是单纯缩小桌面界面。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为目标。界面应支持键盘操作和清晰的焦点状态；正文及控件应达到合格对比度；状态不能只依赖粉色或其他颜色区分；动效应支持减少动态效果；移动端应保持可读字号和足够的触控区域；表格、加载、错误和无结果状态应具备明确语义。
