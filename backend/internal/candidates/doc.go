// Package candidates evaluates and projects immutable per-position candidate
// cores from the current Archive and an explicitly injected collection
// boundary. It owns no HTTP transport, frontend selection state, or image URL.
package candidates
