// Package statistics is the production authority for exact rating,
// series, preference, summary, evidence, and deterministic ordering logic.
//
// The package consumes immutable query and Archive facts. It does not own
// transport shapes, pagination, caches, network access, or mutable state.
package statistics
